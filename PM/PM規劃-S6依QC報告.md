# PM 規劃 — S6：依 QC 健康檢查報告的硬化階段

**日期**：2026-08-16 ／ **規劃**：PM
**依據**：`QC專案健康檢查報告-2026-08-16.md`
**基準**：`12cbda6`（S5-B 已合併，S1–S5 視覺改版完成）

---

## 〇、先講定位：這一階段不是改版，是硬化

S1–S5 做的是**看得見的東西**。QC 這份報告指出的幾乎全是**看不見的東西**：
非同步邊界、狀態競態、送審合規、資料真相來源。

> QC 的結論我認同：**「核心離線功能品質良好、視覺改版已完成，但非同步邊界、
> 餐廳歷史與發布合規仍需一輪硬化。」**

這一階段叫 **S6**，設計端沒有工作。

---

## 一、我抽驗了四條最重的，全部屬實

不採信報告是驗收的規矩，對 QC 也一樣。我挑了嚴重度最高與最容易誤判的四條：

| 項目 | 我的查證 |
|---|---|
| **P0-1** 完成 Task 未被保存 | ✅ `WheelView.swift:40` 的 `Task {}` 沒有存進任何變數；`reset()`（:57）只取消 `tickTask`；`finish()`（:66）只 `guard isSpinning`，**分不出目前轉的是哪一輪** |
| **P1-3** `placemarkNotFound` 跳過 fallback | ✅ fallback 只在 `places.isEmpty` 時執行（:209），搜尋直接 throw 會跳到最外層 catch（:229），**完全繞過 fallback** |
| **P1-5** 缺 privacy manifest | ✅ 全專案找不到 `PrivacyInfo.xcprivacy` |
| **P2-1** 十五公里文案 | ✅ :218、:277 寫死，而 **:432 的註解自己寫著「一度寫死十五公里，但那對『現在要去吃飯』來說太遠了」** |

**P2-1 那條最值得記**：程式碼自己的註解記錄了「不該是十五公里」這個決定，
但**錯誤文案沒有跟著改**。一個決定只改了一半，而改的那一半還留了註解說明 ——
這種缺陷靠讀 diff 抓不到，只有整檔通讀才會發現。

**QC 這份報告可信，其餘項目我按它的分級排程。**

---

## 二、⚠️ P1-4 需要我先做產品決策：餐廳歷史怎麼辦

QC 明講「PM 需先選產品策略」。三個選項：

| | 做法 | 代價 |
|---|---|---|
| 1 | 完整保存餐廳紀錄（source + winnerID + place metadata + SwiftData migration） | 成本最高，**而且保存下來也未必還原得了** |
| 2 | 餐廳 spin 不寫歷史 | 丟掉「我上週去哪吃」這個資訊 |
| **3** | **餐廳歷史只展示，不提供還原** | 少一個功能，但那個功能本來就是假的 |

### 裁示：選 3

**理由一：選項 1 保存的東西會過期。** 店會關、電話會換、座標會搬。
把三個月前的餐廳「還原」出來，使用者拿到的是一份可能已經不存在的清單 ——
那是另一種「假裝知道」。

**理由二：現在那個還原按鈕是死的**，QC 說得準確：「按鈕會安靜地沒有反應」。
**我們對外只講做得到的事** —— 這是「去哪吃」問題單立下的原則，
當時的結論是「找不到就老實說退回」，不是「換個字假裝找到了」。
死按鈕跟靜默退回是同一種病。

**理由三：紀錄本身有價值，還原沒有。** 「上週三晚上抽到某某拉麵店」是一段回憶；
「把那一組候選清單搬回轉盤重抽」不是使用者會做的事。

### 具體要求

- 歷史列**依 source 決定是否顯示還原圖示** —— 餐廳紀錄不顯示
- `SpinRecord` **仍要新增 `source`**（沒有它連「要不要顯示還原」都判斷不了）
- 這一項連帶解掉 P2-4 的一半：**同時加 `winnerID`**，名稱只作顯示

---

## 三、排程：三批，順序不可調

### 第一批：非同步邊界（P0-1、P1-1、P1-2、P1-3）

**這批是唯一會崩潰的一批，先做完再談其他。**

四項的共同根因是同一件事：**沒有辦法分辨「這個結果屬於哪一次請求」。**
轉盤是 generation token，搜尋是 request ID，本質相同。

> 建議工程**一次把身分機制想清楚再動手**，不要四個地方各做一套。

### 第二批：資料正確性與送審（P1-4、P1-5、P2-1、P2-2、P2-3、P2-4）

P1-4 已裁示（第二節）。P1-5 是**上架的硬門檻**，不是品質問題 —— 沒有它送不上去。

### 第三批：回歸防線與文件治理（P3-1、P3-2、P2-5、P2-6）

**P2-6 是我的責任，不必等第三批，我現在就修**（見第四節）。

---

## 四、P2-6 是 PM 的錯，我當場認

QC 指出根 README 與 `Coder/README.md` 的進度已落後實際狀態。**那兩份都是我寫的。**

而且諷刺的是：`Coder/README.md` 我在建立時就寫了「進來先讀哪一份」的導航表 ——
一個**專門用來指路的文件過期了**，比沒有那張表更糟，因為新人會信它。

### 處置

1. **現在就更新**這兩份的狀態段落（不排進第三批）
2. **建立 `PROJECT_STATUS.md` 作為唯一入口**，兩份 README 的進度段落改成指向它
3. **列入 Definition of Done**：每次 PM 驗收通過，更新入口文件才算完成

> 這一條要寫進往後每一張派工單的驗收標準裡。文件治理不是「有空再做」，
> 它跟測試一樣是防止下一個人踩坑的東西。

---

## 五、我自己的流程要改的兩件

QC 的 P3-2 點到了我的做法：**視覺驗收證據放在 `/private/tmp/`，下一個人重現不了。**

這一路我驗收的方式是：實機截圖 → 放大比對 → 取色 → 寫進驗收文件。
**結論寫下來了，證據沒有。** 別人只能相信我，不能檢查我。

| 要改的 | 怎麼做 |
|---|---|
| 驗收證據不可重現 | 精簡的驗收附件（關鍵頁淺深各一）進 repo，或至少保存產圖腳本與操作步驟 |
| 「PM 說通過」是唯一憑據 | 關鍵畫面建立 snapshot baseline（QC 建議 5–8 個），讓回歸由機器接手 |

**另外一件 QC 沒提但我要自己記的**：`6757733` 那次我用 `git add -A` 夾帶了工程
進行中的程式碼。往後我 commit 前**一律先列 staged 清單確認範圍**，
這跟我要求工程「`git diff` 範圍與說明一致」是同一條。

---

## 六、Definition of Done（S6 完成的判準）

沿用 QC 第六節，我加一條：

1. P0 全清
2. P1 全清，或我書面接受風險與替代行為（**P1-4 已經是這種情況**）
3. 43 個既有測試持續通過，**且新增非同步／歷史／持久化測試**
4. Debug、Release、archive 均成功
5. Privacy report 與 App Store validation 通過
6. 實測：快速換條件、切模式、定位拒絕、定位逾時、無搜尋結果、fallback、
   餐廳歷史、深淺模式、reduce motion
7. **入口文件反映同一份最新狀態**（我加的，見第四節）

---

## 七、給 QC 的一句

這份報告的價值不在找到幾個 bug，在於它**驗證了我們驗證不到的層**。

S1–S5 我每一輪都實機驗收、獨立重算對比、逐檔比對 diff —— 但那些方法
**只看得到畫面上的東西**。P0-1 那個競態不會出現在任何一張截圖裡。

> 「快速操作 50 次無崩潰」這種驗收標準，我一次都沒有下過。

這是方法上的缺口，不是誰偷懶。**下一輪的驗收標準要包含「連續操作」與「中途切換」，
不能只有「打開來看對不對」。**

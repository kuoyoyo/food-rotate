# 稽核報告 2026-08-27

**執行**：程式設計師 ／ **委託**：kuoyo（PM）
**範圍**：全專案檢查、修復、菜色資料庫公開、版本號更新
**基準**：`97814cd`（`加 run-foodrotate skill：模擬器驅動`）

---

## 摘要

| | 進場 | 收工 |
|---|---:|---:|
| 全新 derivedData 建置的 Swift 警告（App + Widget） | 0 | 0 |
| **含測試 target 的 Swift 警告** | **3** | **0** |
| 單元測試 | 99 支 / 21 suites | **109 支 / 22 suites** |
| 版本 | 1.0 (1) | **1.1.0 (2)** |
| GitHub 上看得到的菜色 | **0 道** | **50 道全文** |

抓到 **10 項**。修了 7 項，2 項依裁示續掛，1 項是作業風險（已處置）。
其中 **A 是沒有任何人發現過的產品缺陷** —— 兩輪 QC、99 支測試、`FoodDataAudit`
全部沒抓到，因為它們問的都是同一個方向的問題。

---

## A（P1）「素可」是一條死路 —— 選了它必定空轉盤

### 事實

`foods.json` 50 道菜裡，忌口標籤的實際掛載數是：

| 忌口 | 幾道菜 |
|---|---:|
| 無牛 | 32 |
| 無豬 | 16 |
| 無海鮮 | 21 |
| **素可** | **0** |

### 為什麼是 P1 而不是「少一個標籤」

忌口是**硬條件**。`FoodPicker.pick` 的第一步：

```swift
let allowed = library.filter { $0.tags.isSuperset(of: restrictions) }
guard !allowed.isEmpty else {
    return PickResult(items: [], relaxedDimensions: [], emptyReason: .restrictions)
}
```

零命中 ⇒ `allowed` 必為空 ⇒ 直接回 `.restrictions`，**不放寬、不退而求其次**。
於是使用者的完整體驗是：

1. 在篩選器上看到「素可」，點下去
2. 轉盤變成空的
3. 畫面說「忌口條件把所有選項都篩掉了。這一項不會自動放寬，**請把其中一個忌口取消再試**」
4. 他把「素可」取消 —— 那是他唯一選的忌口 —— 才發現要拿掉的就是他真正想要的那個條件

**畫面給的出口對這個人是無效的。** 這不是提示寫錯，是那個選項本來就不該存在。

### 為什麼三道防線都沒攔到

`FoodDataAudit` 驗的是「這道菜有沒有菜系／吃法標籤」，
`FoodDataAuditTests` 斷言真實資料零問題，`FoodPickerTests` 驗的是抽樣規則。

**三者都在問「這道菜有沒有標籤」，沒有人反過來問「這個標籤有沒有菜」。**
問題不在測試寫得差，在於問題的方向沒有被想到過。

### 處置

依 kuoyo 裁示，**從 `FoodTag` 移除**（不補標籤、不新增料理）。
並補上缺的那個方向的防線：

```swift
case restrictionMatchesNothing(tag: FoodTag)
```

**只驗忌口，不驗其餘五個維度**，理由寫在 `FoodDataAudit.matchCheckedDimensions`：
軟標籤零命中只會被逐層放寬，使用者照樣拿得到一盤菜、畫面也會老實說「已放寬 X」——
那是設計好的降級，不是缺陷。在那裡叫一聲只會讓這份檢查開始喊狼來了。

這條檢查放在**新的** `libraryFindings(in:)` 而不是既有的 `findings(in:)`。
分兩個入口是被實作逼出來的：第一版混在一起，結果所有用兩三筆造出來的資料寫的既有測試
**全部誤報**（「三個忌口都沒人掛」）—— 那不是缺陷，那只是一份小清單。
`findings` 問的每件事都是逐筆的，對任何清單都成立；「這個標籤有沒有菜」問的是
**這份清單完不完整**。前提不同就分開，各自的前提寫在名字上。

---

## B（P1，連帶）移除那個 case 會讓使用者的自訂料理**整份消失**

這一項是做 A 的時候發現的，**它比 A 本身更危險**。

`FoodItem.tags` 原本是合成 `Codable`。合成的解碼遇到不認得的 rawValue 會直接 throw，
而這個型別有兩條**已經寫進使用者硬碟**的解碼路徑，兩條都是 `try?`：

| 路徑 | 移除 case 之後會發生什麼 |
|---|---|
| `CustomFoodStore.decode`（UserDefaults） | 整包回 `nil` → `customItems = []`。**他所有的自訂料理一起消失** |
| `SpinRecord.items`（SwiftData） | 回空陣列 → `canRestore` 變 false，還原圖示無聲消失 |

第一條尤其嚴重：他掛「素可」的可能只有一道自訂料理，**賠掉的是全部**。
而且不會當、不會噴、沒有任何錯誤訊息 —— 他只會發現東西不見了。

### 處置

`FoodItem` 改成寬容解碼：不認得的標籤**丟掉那個標籤，不要丟掉那道菜**。

取捨的代價有寫進註解，不含糊：丟標籤的那道菜會從某些篩選結果裡消失，
但那正是 `FoodDataAudit` 在管的事（缺標籤有人接），而「整份清單不見了」沒有人接得住。

**順序是先寫測試、再做寬容解碼、最後才移除 case。**
`TagMigrationTests` 五條，涵蓋單筆與上面那兩條真的會踩到的儲存路徑。

一個細節值得記下來：這支測試的主要斷言**故意不用「素可」**，用的是
`純素`（無論移除前後都不存在的值）。只拿「素可」驗的話，這支在缺陷還在時就是綠的 ——
一支在缺陷還在時就綠的測試證明不了任何事。實測驗證過紅→綠：

```
修之前：✘ Caught error: DecodingError.dataCorrupted ... Cannot initialize FoodTag from invalid String value 純素
修之後：✔ Test run with 5 tests in 1 suite passed
```

---

## C（P2）轉動中改篩選標籤沒被擋 —— 那一輪會靜靜地什麼都不發生

`PROJECT_STATUS.md` 記著「兩種處理都說得通，等 PM 裁示」，這次一併處理。

`FilterBar` 收了 `isSpinning`，但**只接到格數 Picker**。`TagGrid`、「換一組」、
「清除所有條件」三個都沒接。三者都會走 `onChange` → `model.load()` → `pick()`
換掉整份清單，於是轉盤收尾時：

```swift
guard let picked = self.items.first(where: { $0.id == pickedID }) else { return }
```

找不到人，**那一輪轉完什麼都不會發生，而且畫面上沒有任何說明**。

### 處置與紅線

三處都補 `.disabled(isSpinning)`，做法與已核可的格數 Picker 完全一致。

**擋在 UI 層，`RotateViewModel` 一個字沒動。** 這是 `RotateViewModel.wheelSlots`
的註解明文寫下的紅線：在 model 裡加 `guard !spinner.isSpinning` 會讓
`WheelSpinnerRaceTests` 那五十次快速操作重現 P0-1 的路變成一支什麼都沒做的綠燈 ——
等於拆掉一條回歸防線去換一個 UI 層就能給的保護。

「選條件」那顆按鈕**刻意不擋**：它只是收合／展開面板，不動清單。

### 驗證（模擬器實跑，有截圖）

單元測試碰不到 View 接線，所以這一項是實跑驗的：

| 時機 | 觀察到的狀態 |
|---|---|
| 靜止 | 換一組是橘的、33 個標籤都可點 |
| **轉動中** | **換一組灰掉、33 個標籤全部灰掉、格數 Picker 灰掉；選條件仍可用** |
| 轉完並關掉結果頁 | 全部恢復 |

最後一格是刻意驗的 —— 只驗「會不會擋」不驗「擋完會不會還原」，
就可能交出一個永遠鎖著的篩選面板。

---

## D（P3）歷史頁**空的時候**不顯示 SwiftData 降級提示

提示原本寫在 `records.isEmpty` 的 `else` 分支裡，理由是
「空清單時沒有東西會失去，所以不新增損失視窗」。

**那個理由看錯了時態。** `isEphemeral` 講的不是「你剛才失去了什麼」，
是「接下來存不住」—— 而空歷史的人正是**下一筆就要吃虧**的那一個：
他會轉第一次、以為存下來了、關掉 App 才發現沒有。等他有紀錄了才告訴他，已經來不及了。

改成兩個分支共用。`lastSaveFailed` 那一句在空清單時本來就不會出現（沒存過就不會失敗），
所以不會多講一句不是事實的話。

---

## E（P3）「以後都不要的」清單不套改名

`pool` 套了 `renamedNames`，`excludedBuiltIns` 沒套。後果：使用者在轉盤上把
「牛肉麵」改成「阿婆牛肉麵」再設成以後都不要，設定頁的排除清單列的卻是「牛肉麵」——
他要還原時得先認出那是同一道菜。

抽成具名的 `applyingRename(_:)`，兩個消費者共用。補一支測試。

---

## F（P3）`DishListRow<EmptyView>.separatorInset`

為了讀一個純幾何常數要隨便填一個跟它毫無關係的泛型參數。
`PROJECT_STATUS.md` 把它記成「彆扭」記了很久，但 2026-08-21 已經追記過：
它跟 `DishRowArt` 那個「未來的 Swift 版本會是錯誤」的警告是**同一個根**。

搬進檔案層級的 `nonisolated enum DishListRowMetrics`，順帶把散在 body 裡的
兩個魔術數字（24、56）一起收進去。搬出來之後也不必再是 computed ——
泛型型別不能有 static 儲存屬性，這個 enum 可以。

**純重構，零行為變更。** 沒有為它寫測試：這件事的證明是「編得過而且值沒變」，
編譯器已經在做了。硬寫一支 `#expect(separatorInset == 56)` 只會變成一個
把設計 token 焊死的假約束 —— 那個數字本來就該跟著 `Theme.space12` 動。

---

## G（P2）主色有兩份真相，而且兩份都叫「醬」

`PROJECT_STATUS.md` 沒記到這一項。

| 名字 | 值（淺／深） | 在哪 |
|---|---|---|
| 「醬」= `AccentColor.colorset` | `#EF652D`／`#FF7A43` | 色票 JSON、`AccentColorTests` 的字面值、Widget 的 `Color.brand` |
| 「醬」= `DesignTokens.sauce` | `#9B3B2C`／`#D9674F` | 按鈕底、可點文字、轉盤指針 |

**同一個名字指著兩個值，而且第一個有三份手抄本。**
Widget 那份的註解自己寫著「這是複本，改一邊就要改另一邊」——
一個要人記得的約定，就是一個遲早會斷的約定。

### 處置：不是統一成一個值，是讓兩個值各只有一份

它們**本來就該不一樣**，壓成同一個會壞掉其中一邊：`#EF652D` 配 `onSauce` 的白
只有 3.2x（不合格），而 `#9B3B2C` 當全域 tint 在分頁列上暗到像停用。
一個受「小面積上要認得出是這個 App」約束，一個受 AA 對比約束。

所以：新增 `DesignTokens.accent` / `Dark.accent`，並在註解裡用一張表寫死
它跟 `sauce` 的分工，免得下一個人「順手統一」掉。`AccentColorTests` 的字面值
改成從 token 推導 —— 那支既有的「色票資產本身沒有被改掉」因此升級成
**色票 JSON 與 Swift token 的雙向同步檢查**（色票沒辦法引用 Swift，
它們的一致性只能由測試保證）。

**畫面零變化**，數值驗證過：

```
淺色 舊手抄 (0.937, 0.396, 0.176)
淺色 新 token (0.9372549..., 0.3960784..., 0.1764705...) -> 相同 ✅
深色 舊手抄 (1.0, 0.478, 0.263)
深色 新 token (1.0, 0.4784313..., 0.2627450...) -> 相同 ✅
```

（舊的手抄本是四捨五入到小數三位的，新的是 hex 精確值。8-bit 量化後同一個顏色。）

---

## Widget 拿不到 token（`PROJECT_STATUS.md` 掛著的一項）

G 的第三份手抄本要真正消掉，Widget 就得讀得到 `DesignTokens`。
`PROJECT_STATUS.md` 記著「要搬 `Shared/` 會動 pbxproj」。

實際做法**沒有搬檔案**：加一個 `PBXFileReference`（`SOURCE_ROOT` 相對路徑）
與一個只掛給 Widget 的 `PBXBuildFile`。`DesignTokens.swift` 留在原地，
App 那邊照舊靠同步群組收，路徑沒變，所有文件與 `Tools/make-icon.swift` 的引用都不用改。

驗證不是「建置過了」而是**兩個 target 的編譯清單各看一次**：

```
Widget target 的 SwiftFileList：
  Shared/GenerationActivityAttributes.swift
  FoodRotateWidget/LoadingLiveActivity.swift
  FoodRotate/DesignTokens.swift          ← 進來了
App target 裡 DesignTokens 出現次數：1  ← 沒有被編兩次
```

**Live Activity 的實際渲染沒有實機驗證。** App 在前景時島上不會顯示自己的
Live Activity，模擬器要跑到那個畫面需要定位與網路。改成用上面那組數值比對 +
編譯歸屬確認代替 —— 兩者合起來能證明「讀得到 token」與「值沒變」，
但**不等於有人看過島上那條進度條**。要完全確定，請在實機跑一次「去哪吃」。

---

## 額外抓到：「Swift 警告 0」這個說法本身沒有被驗證過

`PROJECT_STATUS.md` 寫著「全新 derivedData 建置 Swift 警告 0 —— 增量建置沒噴不算數」。

**這句話本身是用 `xcodebuild build` 量的，而那個指令根本不編測試 target。**
改用 `xcodebuild test` 量，進場時就有 3 個警告，從 S6 掛到現在沒人看到：

```
FoodRotateTests/WheelSlotsEntryTests.swift:36:9: warning: no calls to throwing functions occur within 'try' expression
FoodRotateTests/WheelSlotsEntryTests.swift:54:9: warning: 同上
FoodRotateTests/WheelSlotsEntryTests.swift:76:9: warning: 同上
```

已清掉（三支測試的 closure 都沒有 throwing 呼叫，`try`／`throws` 是多餘的；
helper 維持 `rethrows`）。理由寫進了那個 helper 的註解，免得再被加回來。

這一項的教訓跟 A 是同一條：**檢查本身也要被檢查**。
一個量錯範圍的「零警告」跟沒有量過的差別，只在於它讓人停止懷疑。

---

## 順帶更正兩處文件

`.claude/skills/run-foodrotate/SKILL.md`：

1. `test` 的支數 99/21 → 109/22
2. **「`-autoSpin` 啟動參數已經不存在了」是錯的。** 原本的判斷依據是
   「程式碼裡沒有任何 `ProcessInfo` 讀取」—— 那個判斷**找錯 API 了**：
   `-autoSpin` 與 `-startTab` 讀的是 `UserDefaults.standard`
   （`RotateView` 的 `#if DEBUG .task`、`RootView.AppTab.launchArgument`），
   而 iOS 會把 `-key value` 形式的啟動參數自動放進 `UserDefaults`。兩者都還在。

---

## 沒有修的，與為什麼

| # | 項目 | 為什麼不修 |
|---|---|---|
| H | `foods.json` 的 `category` 混了「輕食」「鍋物」兩個吃法值（`poke-bowl`／`low-calorie-bento`／`mala-hotpot`／`lamb-hotpot` 四道）。`FoodLibrary.categories` 因此把吃法值餵進新增料理表單的「分類」Picker，而該頁 footer 承諾「選了分類就會自動掛上對應的**菜系**標籤」 | `PM/PM提案-7道菜補標籤-待核可.md` 第四節 2026-08-11 已記「這是既有狀況，這次不動 —— 它會顯示在卡片副標上，改了是視覺變更」。裁示沒有改變，**這次也不夾帶**。不過它現在**看得見了**：`docs/菜色資料庫.md` 會在那四道旁邊標「分類寫「鍋物」」 |
| I | 缺 `PrivacyInfo.xcprivacy`（上架硬門檻，QC 2026-08-16 P1） | kuoyo 本次裁示續掛，等送審決定 |

---

## ⚠️ 一筆作業風險（已處置，但要記著）

接手時工作區有一筆**未提交**的 `project.pbxproj` 改動，把 `DEVELOPMENT_TEAM`
從 `""` 改回真實 Team ID。而 `676fe99`「公開前清理」正是為了拿掉它，
**且這個 repo 是 PUBLIC**。

`PROJECT_STATUS.md`「一筆要記的帳」記著上次 `git add -A` 夾帶程式碼的事故 ——
這次的陷阱形狀一模一樣，只是夾帶的東西從程式碼變成一個不該公開的識別碼。

處置：**全程明列路徑提交，一次都沒有用 `git add -A`**；提交 pbxproj 之前先把該行
還原成 `""`，提交後再把本機值改回去。收工時工作區在這一點上跟接手時一致。
每個 commit 都驗過 `git diff --cached | grep -c <TeamID>` = 0。

**這筆本機改動會一直在。** 它是本機簽章需要的，不該進 repo；下一個人接手時
還是會看到它，請照樣不要提交。

---

## 菜色資料庫（kuoyo 指定交付）

**問題**：README 說「50 道內建料理」，但 GitHub 上沒有任何地方看得到那 50 道**是什麼**。
唯一的來源是 `FoodRotate/Core/foods.json`，一份 20KB 的原始 JSON。

**交付**：[`docs/菜色資料庫.md`](docs/菜色資料庫.md)，236 行。

- 50 道**全部列出**，依菜系分節。每道給 emoji、菜名、id、線稿圖示、
  吃法／情境／口味標籤、忌口標籤，以及**全部優點與全部缺點的全文**
  （250 則，一則都沒有截斷或摘要 —— 已用程式逐則比對過）
- 一張**標籤覆蓋率表**（六個維度、每個標籤幾道菜）。這張表就是 A 那個缺陷的顯影劑，
  零命中會標 `**0** ⚠️`
- 菜系分布表、刻意沒有菜系的豁免與理由、`FoodDataAudit` 的稽核結果

**它是產生的，不是手寫的副本** —— `Tools/make-food-catalog.swift`，
跟 App **編同一批 Core 型別**（`FoodItem`／`FoodTag`／`FoodIcon`／`FoodDataAudit`）。
所以「哪道菜配哪個圖示」不是文件裡重寫一遍的規則，是直接問 App 本人。
同一條理由見 `Tools/make-icon.swift`：色值不烙進 SVG，讀同一份 `DesignTokens`。

驗證方式（可自己重跑）：刪掉輸出檔重跑產生器，`git diff` 必須是空的。
已驗，兩次的 sha 相同。

---

## 版本 1.1.0 (2)

六組 build configuration 全部改到（App × 2、Widget × 2、Tests × 2），
`grep -c` 驗過 6/6/0。**並且驗到產物本身**，不只是專案檔：

```
FoodRotate.app             CFBundleShortVersionString 1.1.0 / CFBundleVersion 2
FoodRotateWidgetExtension  CFBundleShortVersionString 1.1.0 / CFBundleVersion 2
```

---

## 驗證總表 —— 跑過的與沒跑的分開講

### 跑過的

| 項目 | 結果 |
|---|---|
| 全新 derivedData 建置（App + Widget） | BUILD SUCCEEDED，Swift 警告 0 |
| 全新 derivedData 測試（含測試 target） | **109 tests / 22 suites passed**，Swift 警告 0 |
| B 的紅→綠 | 修前 5 支紅（`DecodingError.dataCorrupted`），修後全綠 |
| A 的防線 | `libraryFindings` 對造假資料會叫、對真實 `foods.json` 安靜；且驗過逐筆入口不准碰這條檢查 |
| 八條產品規則零變化 | `FoodPicker.swift` **一行未改**，`FoodPickerTests` 原樣全過 |
| C（轉動中） | 模擬器實跑 + 三張截圖：靜止／轉動中／轉完還原 |
| A（素可） | 模擬器實跑，忌口區實際只剩 無牛／無豬／無海鮮 |
| G（主色沒變） | `AccentColorTests` 三條全過（含色票資產比對）+ 數值逐項比對 |
| Widget 讀得到 token | 兩個 target 的 SwiftFileList 各看過，App 端沒有重複編譯 |
| 版本號 | 專案檔 6/6/0，且從建置產物的 Info.plist 讀出 1.1.0 (2) |
| 菜色資料庫可重現 | 刪檔重跑，sha 相同；50 道與 250 則優缺點逐項比對無缺漏 |

### 沒跑的（誠實列出）

| 項目 | 為什麼 |
|---|---|
| **Live Activity 島上的實際顏色** | App 前景時島上不顯示自己的 Live Activity，模擬器跑到那個畫面需要定位與網路。改用數值比對 + 編譯歸屬確認代替。**這不等於有人看過那條進度條** |
| **D（歷史頁空狀態的降級提示）的實際渲染** | 要看到它必須讓 `ModelContainer` 真的建不起來。`HistoryStorage` 的檔頭寫明「不要去製造真實的儲存失敗，要加測試就加在狀態機那一層」，所以沒有為了截一張圖去違反那條決定。狀態機（`notice` 兩種文案）有 `HistoryStorageTests` 背書，這次改的是**它顯示在哪個分支**，靠讀程式碼驗 |
| **實機（非模擬器）** | 手上沒有實機。深色模式、Dynamic Type、觸覺回饋都只在模擬器驗過 |

---

## 改了哪些檔

```
 17 個既有檔案，+385 −101
 新增：FoodRotateTests/TagMigrationTests.swift
       Tools/make-food-catalog.swift
       docs/菜色資料庫.md（產生的）
       稽核報告-2026-08-27.md（本檔）
```

---

## 追記：菜色資料庫也要進 App（kuoyo 當日追加）

交出上面那份報告之後 kuoyo 指出：**菜色資料庫沒有顯示在 App 裡面。**

他是對的，而且這件事比我原本認知的更接近本體。回頭看設定頁：

```
菜色資料庫
  我的清單     改了 4 項  ›
  目前可抽        50 道
  內建料理  50 道・12 類        ← 沒有 ›，點不進去
```

**「內建料理　50 道・12 類」是一列講得出數量、看不到內容的死資訊。**
跟 README 之前的毛病一模一樣 —— 我修了 README 那一份，卻沒發現 App 裡有一模一樣的
一列。而 App 才是使用者真正會待的地方。

這也是死按鈕那條原則的另一面：死按鈕是「看得到、按不動」，這是
「講得出數量、看不到內容」。兩者都是畫面上有一個承諾而背後沒有東西接。

### 做了什麼

`FoodRotate/Views/FoodLibraryView.swift`，從那一列點進去：

- 依**菜系標籤**分組（不是 `category` —— 照 `category` 分會冒出「輕食」「鍋物」
  兩個假菜系，把 H 那個資料問題變成畫面問題）
- 每一列：線稿圖示 + 菜名 + 菜系／吃法角標 + 忌口副標，重用 `DishListRow`
- 點開：**六個維度的完整標籤**（忌口那行用警示色，跟篩選器同一條）+ 優缺點全文，
  配色與符號跟候選清單、結果頁同一套
- 搜尋：菜名**與標籤**都比對。「打『無牛』要找得到什麼」才是使用者真正的問題
- **唯讀。** 改名、排除、刪除全部不在這裡，它們在「我的清單」——
  這一頁回答「這個 App 內建了什麼」，那一頁回答「我改了什麼」

順帶修掉那個「12 類」：它數的是 `category` 的相異值，而那欄混了兩個吃法值，
**實際菜系是 10 種**。這一列跟它點進去的那一頁必須是同一個數字。

### 搜尋規則放進 `Core/`，不留在 View

`FoodSearch.matches(in:query:)`。理由跟 `FoodPicker`／`FoodIcon` 一樣：
「打『無牛』要不要找得到牛肉麵」是**產品規則**，不是畫面的事。
而且留在 View 的 computed property 裡**測不到**——搜尋恰好是那種
「看起來能動、但少一條路就靜靜漏掉結果」的東西，漏掉的那幾道菜不會有人抗議。

`FoodSearchTests` 七條，**大部分跑真實的 `FoodLibrary.all`**：
兩筆假資料驗得到「filter 寫對了嗎」，驗不到「使用者實際會打的字在實際出貨的
50 道菜上找不找得到東西」。其中一條釘著一個容易寫錯的地方：

> 打「無牛」回 32 道，而且**牛肉麵不在裡面** ——
> 它沒有無牛標籤，不能因為名字裡有「牛」就被撈進來。

### 驗證

| 項目 | 結果 |
|---|---|
| 全新 derivedData 建置＋測試 | **116 tests / 23 suites passed**，Swift 警告 0 |
| 搜尋 | `FoodSearchTests` 七條，跑真實資料 |
| 實際畫面 | 模擬器實跑：設定頁的列有 `›` 了、點得進去、50/50、台式 13 道、展開看得到六維度標籤與優缺點 |
| 深色模式 | 實跑截圖，token 正確（忌口用深色版 negative、優點用深色版 positive） |
| 不重複講同一件事 | 展開時收掉忌口副標 —— 下面的維度表已經有一行「忌口　無豬・無海鮮」 |

### 順帶澄清「忌口沒有刪除？」

`FoodTag` 裡的 `vegetarianFriendly = "素可"` 這個 case **已經刪掉了**，
忌口維度現在只剩 `無牛`／`無豬`／`無海鮮`，實機截圖可以看到篩選器的忌口區只有三顆。

repo 裡還搜得到「素可」兩個字，但全部是**註解與紀錄**，一行程式碼都沒有：
`FoodTag.swift` 記著它為什麼被移除、`FoodItem.swift` 記著移除它踩到的解碼問題、
測試與本報告記著當時的事實。那是刻意留的 ——
**被指正的地方要留下錯誤紀錄與正確版本，否則下一個人照舊稿又改壞一次。**
要連紀錄一起清掉的話，那是另一個決定，講一聲就做。

# 食物轉盤 Food Rotate — 專案現況彙整（設計交接用）

**整理日期**：2026-08-11 ／ **整理人**：PM
**用途**：給設計師做視覺與體驗優化的現況底稿。本文件只描述「現在長什麼樣、為什麼這樣」，不含任何程式改動。

---

## 一、產品一句話

不知道吃什麼的時候，點幾個條件、轉一次盤，它幫你決定；決定完可以順手找到附近的店導航過去。

| 項目 | 現況 |
|---|---|
| 平台 | iOS（SwiftUI，iOS 26 SDK），單一 App + Widget Extension |
| 語言 | 繁體中文（無多語系） |
| 資料 | 內建 50 道台灣常見主食，**離線可用** |
| 分類與標籤 | ⚠️ **2026-08-11 更正**：本文原寫「12 個菜系分類」，**不準確**。`category` 欄位有 12 個相異值，但其中「輕食」「鍋物」是吃法不是菜系。**篩選實際用的是標籤：10 個菜系 × 7 個吃法**（不是 12×6）。畫圖示請以此為準 |
| 網路／定位 | 只有「去哪吃 / 找附近」會用到，其他功能全離線 |
| 開發狀態 | 三階段功能已完成，iPhone 17 Pro 模擬器實跑驗收通過 |
| 下一步 | **視覺與體驗優化（本次設計任務）**；Android 移植為之後的事 |
| 近期大改 | 已移除語言模型，改為「內建資料庫 + 標籤篩選」，約 -3000 行 |

---

## 二、資訊架構（畫面清單）

底部 3 個 Tab。以下是全部畫面，右欄是設計可著力的地方。

| # | 畫面 | 進入方式 | 目前構成 | 設計可動範圍 |
|---|---|---|---|---|
| 1 | **首次啟動說明** WelcomeView | 首次開啟自動彈出 | 🎡 emoji + 標題 + 4 條說明（點條件／瞬間有結果／有優缺點／順便找店）+「開始用」按鈕 | 高。目前是純文字條列，可做插畫或引導動線 |
| 2 | **轉盤（主畫面）** RotateView | Tab 1 | 由上到下：模式切換 → 篩選列 → 狀態提示 → 轉盤 + 開始轉 → 候選清單 → 版權 | **最高優先**，見第三節 |
| 3 | **轉盤結果** ResultSheet | 轉完自動彈出（全屏 sheet） | 大 emoji 76pt + 「今天就吃」+ 菜名 + 分類膠囊 → 優點卡 → 缺點卡 → 找附近／導航按鈕 →（店家）打電話 | 高。這是情緒高點，目前偏樸素 |
| 4 | **附近的店** NearbyRestaurantsView | 結果頁或卡片「找附近」 | 上方地圖 260pt + 下方店家清單（店名／地址／距離） | 中 |
| 5 | **歷史** HistoryView | Tab 2 | 清單：emoji + 中選菜名 + 條件摘要 + 時間；點一筆還原整組清單；右上選單可清空 | 中。空狀態用系統 ContentUnavailableView |
| 6 | **設定** SettingsView | Tab 3 | Form 四段：轉盤格數／菜色資料庫（我的清單、可抽道數）／地圖偏好／關於 | 低。系統 Form 樣式 |
| 7 | **我的清單** MyListView | 設定 → 我的清單 | 我加的料理、以後都不要的（可還原）、還原成預設 | 中 |
| 8 | **新增／編輯料理** FoodEditorView | 我的清單 → 新增 | Form：名稱、Emoji、分類、六維度標籤勾選、優點、缺點 | 中。標籤用逐列打勾，較冗長 |
| 9 | **Live Activity／靈動島** | 只在「去哪吃」搜尋時 | 進度條（時間估計）+ 階段文字（定位中／搜尋中／完成 N 家） | 中 |

---

## 三、主畫面（轉盤）組成細節

這是使用者 90% 時間停留的畫面，優化重點。

| 區塊 | 目前做法 | 已知問題／可優化點 |
|---|---|---|
| 模式切換 | segmented control：「吃什麼」／「去哪吃」 | 兩個模式差異大（一個離線瞬間、一個要等），視覺上只是一個小分段控制 |
| 篩選列 FilterBar | **預設收合**。收合時顯示條件摘要膠囊（如「日式・無牛」）+「換一組」骰子鈕；展開後每個維度一列橫向捲動的膠囊標籤 | 展開後很長；六個維度攤開會吃掉整個第一屏（這也是收合的原因）。標籤數量多（33 個） |
| 狀態提示 NoticeBox | 四種互斥提示：找不到店／忌口把選項篩光／已放寬條件／只湊得出 N 格 | 目前四種都長一樣（圖示 + 標題 + 說明），只有 tint 不同（橘／藍） |
| 轉盤 WheelView | Canvas 繪製，最大 360×360pt。8 色循環扇形、白色描邊、投影；文字沿半徑排列、超過 5 字截斷；中央「轉」圓鈕；頂端三角指針（主色） | 菜名截斷成 4 字 + … 是明顯痛點；12 格時字很擠；配色是功能性的，未經視覺整理 |
| 開始轉按鈕 | 膠囊 borderedProminent，全寬 | — |
| 候選清單 FoodCardList | 「候選清單 N 道」標題 + 每道一張卡。卡片收合時：emoji 圓底 + 菜名（中選加「轉中」膠囊）+ 分類 + 展開箭頭；展開後：優點（綠）／缺點（橘）條列 + 三顆膠囊鈕「找附近／改名／不要」 | 卡片資訊密度低，8–12 張卡要滑很久；中選卡片只用主色外框標示 |

---

## 四、現有視覺資產與樣式

**目前沒有設計系統**，全部沿用 SwiftUI 系統元件與系統色。以下是僅有的自訂值。

| 項目 | 值 |
|---|---|
| 主色 AccentColor（淺色） | `#EF652D`（橘紅） |
| 主色（深色模式） | `#FF7A43` |
| 頁面底色 | `systemGroupedBackground`（系統值，深淺自動） |
| 卡片底色 | `.background` / `secondarySystemGroupedBackground` |
| 轉盤 8 色盤 | `#FA7359` `#FCB84D` `#8CC770` `#5CB8BF` `#738FE6` `#B887E0` `#F28CAD` `#E6A16B` |
| 圓角 | 卡片 14、提示框 12、載入框 16；按鈕一律膠囊 |
| 字體 | 全部系統字（headline / subheadline / caption…），未指定自訂字型 |
| 圖示 | 全部 SF Symbols |
| 插圖 | **無**。所有食物視覺都靠單一 emoji |
| App Icon | 已有一張 `AppIcon.png`（由腳本生成，非設計稿） |
| 深色模式 | 靠系統語意色自動支援，**未逐頁檢查過** |

---

## 五、動效與觸覺現況

| 項目 | 現況 |
|---|---|
| 轉盤轉動 | 自訂緩動（非系統動畫），固定 4.2 秒，隨機轉 5–7 圈 + 落點抖動 |
| 觸覺 | 每跨過一格一次 tick（強度隨速度遞減）、停止時一次較重回饋、按鈕點擊回饋 |
| 篩選展開／卡片展開 | `.snappy(0.22)` |
| 載入（僅去哪吃） | 線性進度條 + 階段文字；進度條長度是「上次實際花的秒數」估計值，非真實進度 |
| 結果呈現 | 直接彈出 sheet，**沒有中選瞬間的慶祝動效** |

---

## 六、設計時必須遵守的產品規則（不可改）

這幾條是產品決策，視覺可以重做但行為不能改。

| 規則 | 原因 |
|---|---|
| **忌口（無牛／無豬／無海鮮／素可）永遠不會被自動放寬** | 湊不滿格數寧可轉盤只有 4 格，也不能讓不吃牛的人看到牛肉麵 |
| 收合狀態必須看得到目前條件摘要，不能只顯示「篩選」兩字 | 否則使用者會以為 App 隨便給 |
| 放寬條件時要老實說明放寬了哪個維度 | 不能默默給不符合的東西 |
| 「去哪吃」模式只顯示菜系那一列標籤 | 其餘維度對地圖搜尋無效，列出來卻不作用比不列更糟 |
| 店家**不顯示營業狀態** | MapKit 沒有這個資料，不假裝知道；改以顯眼的「打電話問」替代 |
| 轉盤最少要留 2 格才可刪卡片 | 少於 2 格轉不起來 |
| 轉盤格數只能是 4／6／8／10／12（預設 8） | 再多字放不下 |
| 搜尋距離只能是 1／3／5／10 公里（預設 5） | 更遠就不是「今天中午去吃」的距離 |

---

## 七、已知痛點（建議優先處理）

依 PM 判斷排序，設計師可自行調整。

| 優先 | 痛點 | 說明 |
|---|---|---|
| P0 | 轉盤菜名截斷 | 超過 5 字就切成「4 字…」，「蚵仔煎」沒事但「麻辣鴨血豆腐鍋」就看不懂 |
| P0 | 結果頁情緒平淡 | 轉盤是有期待感的互動，結果卻是一張安靜的表單頁，缺少中獎瞬間 |
| P0 | 第一屏資訊爭搶 | 模式切換 + 篩選列 + 狀態提示 + 轉盤，轉盤常被推到摺線下 |
| P1 | 33 個標籤的選擇負擔 | 六個維度橫向捲動，使用者不容易掌握全貌，也不知道哪些組合有東西 |
| P1 | 候選清單過長 | 8–12 張卡片全展開要滑很久，且與轉盤資訊重複 |
| P1 | 全靠 emoji 沒有插圖 | 食物 App 缺少食慾感；轉盤色盤是功能性配色，非美術配色 |
| P2 | 四種狀態提示長得一樣 | 「已放寬」是資訊、「忌口篩光」是要使用者行動，權重應該不同 |
| P2 | 深色模式未逐頁驗過 | 轉盤 8 色盤在深色底下的對比未確認 |
| P2 | 新增料理表單冗長 | 33 個標籤逐列打勾 |
| P2 | App Icon 是腳本產物 | 需要正式設計 |

---

## 八、需要設計師回覆／決定的事

1. 是否要建立一套設計系統（色票、字級、間距、元件），還是維持系統原生風格只做局部優化？
2. 轉盤是否改用插圖／圖示取代 emoji？若是，50 道菜的美術量產方式要一起評估。
3. 中選瞬間要什麼程度的慶祝（動效／音效／觸覺）？現在只有一次觸覺回饋。
4. 篩選器要不要重新設計互動模式（例如改成獨立頁面、或分層漸進式提問）？
5. App Icon 與品牌（名稱字標）是否納入本次範圍？

---

## 九、參考文件

| 文件 | 內容 |
|---|---|
| `大改紀錄-拿掉語言模型.md` | 本次架構大改的原因、取捨、每個決策的理由（含篩選放寬邏輯、店家資料限制） |
| `DEVLOG.md` | 更早期的開發歷程（含已移除的語言模型階段） |
| `FoodRotate/Core/foods.json` | 50 道料理的完整資料（名稱、emoji、分類、標籤、優缺點） |
| `taiwan_main_dishes_50.txt` | 原始料理資料來源 |

# PM 驗收 — S1（設計系統打底）**通過**

**日期**：2026-08-11 ／ **驗收**：PM
**範圍**：S1a 設計規格 + S1b 工程實作
**基準點**：`6b2a37f` ／ **驗收時 HEAD**：`26d1d3b` + 未提交的 token 收尾

---

## 一、結論

**S1 通過，可以進 S2。** 兩份完工報告我沒有採信，全部自己重跑。

---

## 二、我實際做的驗證（不是看報告）

| # | 驗證項目 | 方法 | 結果 |
|---|---|---|---|
| 1 | 對比數值 | 自己實作 WCAG 相對亮度公式，重算全部 | ✅ **與規格一位不差**（16 組） |
| 2 | 淺階／icon 推導公式 | 自己跑 HSB 換算 | ✅ 三處出入**全部確認工程是對的**，見第四節 |
| 3 | 多重吃法優先序 | 實跑 `foods.json` 逐道套規則 | ✅ 12 道無一取錯 |
| 4 | 資料完整性 | 實跑：缺吃法 0 道、缺菜系 1 道（低卡餐盒，已豁免） | ✅ |
| 5 | 單元測試 | `xcodebuild test` 親自執行 | ✅ **18 個全過，TEST SUCCEEDED** |
| 6 | 建置 | `xcodebuild build` | ✅ BUILD SUCCEEDED |
| 7 | 實機淺色 | 模擬器 iPhone 17 Pro 實跑截圖 | ✅ 見第三節 |
| 8 | 實機深色 | 同上 | ✅ 見第三節 |
| 9 | 產品規則零變化 | `git diff` 逐檔比對 | ✅ 見第五節 |

---

## 三、實機確認的兩件事

**淺色模式**：6 格取色 `0,2,3,4,6,7`（辣椒／抹茶／蔥綠／青瓷／芋泥／焙茶）。
**抹茶那格（美式牛排）是墨字，其餘五格白字** —— 對比修正在真實畫面上確實生效，不只是規格上的數字。

**深色模式**：盤色換成深底那套，**六格全部墨字**，與規格二-3 一致。

跳號取色的效果看得出來：相鄰兩格色相拉得開，沒有兩塊靠在一起分不出來。

---

## 四、三處推導出入：**工程是對的**

工程沒有照抄規格表，而是自己用公式重算，抓到三處不符。我獨立驗算，**三處判斷全部正確**：

| 項目 | 規格表 | 公式（工程＝PM） | 判定 |
|---|---|---|---|
| icon 蔥綠 | `#456A56` | `#456D57` | 四捨五入雜訊，不影響 |
| icon 青瓷 | `#3B6A76` | `#396B78` | 同上 |
| **蔥綠淺** | `#7BA88E` | `#6F9882` | **規格表值是拿微調前的舊蔥綠推的** |

**第三處的證明**：把**微調前**的舊蔥綠 `#5E8C72` 代入 ×0.82／×1.20，**分毫不差得到 `#7BA88E`**。
規格第六節的 icon 表有跟著第一節的微調重算，但二-3 的淺階表漏了同步。

**採用公式、不採用表值** —— 工程的三個理由成立，尤其第二點：把 `#7BA88E` 抄成常數等於又多一份跟母盤脫鉤的色票，那正是這輪要解掉的問題。`#6F9882` 的墨字對比 5.09，仍過 AA。

> **仍要請設計師確認一件事**（不擋 S2）：`#6F9882` 比 `#7BA88E` 更接近母色 `#557F68`。
> 淺階變體的用途是在 10／12 格時與母色**區分開**，請確認縮小後的差距在 12 格上還夠分辨。
> 若不夠，請在規格裡把它標記為「不由公式推導」並給明確常數與理由。

---

## 五、八條產品規則：行為零變化

`git diff` 逐檔確認，**產品規則所在的檔案完全沒有被動**：

```
FoodRotate/Core/FoodPicker.swift      0 行變更
FoodRotate/Views/FilterBar.swift      0 行變更
FoodRotate/Views/FoodEditorView.swift 0 行變更
FoodRotate/Core/FoodItem.swift        0 行變更
```

`FoodTag.swift` 只有 +12 行（新增「肉食」case 並歸到 `.form`），是核可過的產品語彙擴充。
沒有夾帶：兩份報告的第六節都主動列出「看到但沒有順手改的」，與 diff 對得起來。

---

## 六、特別記一筆的三個判斷

1. **對比門檻寫成測試**（五種格數 × 深淺兩底逐格驗 ≥ 4.5）。理由「對比不合格是看不出來的那種壞，不能靠下次有人想到要量」完全正確 —— 這件事本身就是這輪被抓出來的，寫成測試才不會再發生。
2. **豁免用「id → 理由」字典而非 Set，而且會自己喊過期。** 強迫寫理由，清單才不會變成垃圾桶。
3. **圓角維持大小命名**，語意寫進註解。理由（同一個值服務多種語意，改叫 `radiusCard` 之後「提示框為什麼用卡片圓角」要一直解釋）成立。

---

## 七、裁示兩件待決

| # | 事項 | 裁示 |
|---|---|---|
| 1 | 蔥綠淺用公式值還是表值 | **用公式值 `#6F9882`**，但請設計師確認第四節的可分辨性 |
| 2 | icon 八格改六格，現在做還是 S5 | **現在做** |

**第 2 項的理由**：現有 `AppIcon.png` 還是用**舊色盤**畫的，跟 App 內已經全部換掉的顏色互相矛盾。既然無論如何都要重跑一次腳本，就一次做到位 —— 六格取色序列已經實作好了（`slotOrder(for: 6)`），加上中心刀叉放大到 40%，一次產出。S5 的「完整重繪」是設計層面的重新設計，跟這次的「用正確顏色重產」不是同一件事。

工程的保守讀法（只做色彩、構圖留到 S5）**符合工作約定，處理正確** —— 說法不一致時請示而不是自己選，這是對的。不一致是我的文件造成的：核可文件第一節核可了六格，第五節的指示卻沒寫進去。

---

## 八、Commit 放行

kuoyo 已同意。工程的 commit 計畫照原樣執行，**外加 icon 重產**：

```
FoodRotate/DesignTokens.swift
FoodRotate/Theme.swift
FoodRotate/Views/WheelView.swift
Tools/make-icon.swift
FoodRotateTests/DesignTokenTests.swift
FoodRotate/Assets.xcassets/AppIcon.appiconset/AppIcon.png   ← 新增：六格 + 新色盤重產
```

文件（`Design/` 三份 + 根目錄三份回報 + 本文件）**另外一筆**，跟程式分開。

# 工程回覆 — S2 進度（給 PM）

**日期**：2026-08-11 ／ **提出**：程式設計師
**依據**：`工程交接-給程式設計師.md` 第六節 S2、`Design/設計規格-Theme-v1.md` 七／八節
**基準點**：`47a0edd`（S1 尾的 icon 已進版）
**狀態**：三項中**兩項完成**、一項**卡在資產**。未 commit，等 PM 同意

---

## 一、三項驗收標準的狀態

| # | 標準 | 狀態 |
|---|---|---|
| 1 | 兩行排版 + 動態字級，4／6／8／10／12 全部零截斷 | ✅ **完成**，實機驗過 12 格滿載 |
| 2 | 圖示系統取代 emoji（含自訂料理與店家 fallback） | ⚠️ **對應層完成，卡在 9 個 SVG 還沒交** |
| 3 | 中選瞬間：提亮放大 0.35s、其餘降透明度、success 觸覺 | ✅ **完成**，實機驗過 |
| + | 圖示不負責辨識「是哪一道菜」 | ✅ 已成為圖示系統的前提，寫進程式註解與測試 |

---

## 二、兩行排版（完成）

規則照規格第七節：**四字以內一行、五字以上兩行（前行取一半），永遠不截斷。**
各格數的字級／行距／圖示尺寸／半徑位置照那張表，以半徑 150pt 為基準等比縮放。

### 實機：12 格滿載

我把模擬器的格數調到 12 抓了實際畫面。盤上同時出現：

- **韓式泡菜豆腐鍋**（7 字，最壞情況）→「韓式泡菜／豆腐鍋」
- 韓式石鍋拌飯 →「韓式石／鍋拌飯」・清燉羊肉爐 →「清燉羊／肉爐」・韓式人參雞湯 →「韓式人／參雞湯」
- 低卡餐盒、美式漢堡、美式牛排、台式炒飯、蚵仔煎、滷肉飯、爌肉飯、水餃 → 各一行

**零截斷，沒有任何一個省略號。** 同一盤上 12 種顏色沒有兩塊重複（8 母盤 + 4 淺階變體），
墨字白字各自跟著格子走。

### 一處我沒有照規格做，而且我認為規格那條是錯的

> 規格第七節：「左半邊翻轉 180° 的既有處理保留，**兩行的行序也要跟著翻**，
> 否則左半邊會上下顛倒讀。」

**我沒有翻，因為翻了才會顛倒。** 行距是加在**旋轉後**的座標系上的，而字面也是在同一個
座標系裡轉正的 —— 兩者一起鏡射，讀起來仍然是第一行在上。規格那句話只有在
「用全域座標算行位置」的實作上才成立。

不是紙上推論：實機截圖上左半邊的「韓式人／參雞湯」「印度香／米飯」「美式烤／肋排」
都是正的。**請設計師確認這一條，規格文字建議修掉**，否則下一個人照著改會改壞。

---

## 三、中選轉場（完成）

中選那格**連同它的字整個重畫一次再放大**，其餘 11 格壓暗。

不是只在中選格疊一層白 —— 那樣字會被蒙上一層霧，反而比周圍更難讀。

**success 觸覺本來就有**：`WheelSpinner.finish()` 裡的 `Haptics.wheelStopped()` 用的就是
`.success`，我沒有再加一個，重複給會變成兩下。

### 一個必要的時序改動，請 PM 確認

原本 `winner` 與 `showResult` 是**同一瞬間**設定的，結果頁是全螢幕 sheet ——
那 0.35 秒的提亮放大會被整個蓋掉，**等於沒做**。

所以我把結果頁延後 0.35 秒開。實作上不另外存計時狀態：醒來時比對 `winner` 還是不是
同一道就好，使用者在這 0.35 秒內又按了轉或改了條件，這個 Task 自己就不會開錯的結果頁。

秒數收在 `WheelCelebration.duration`，轉場與延後開頁**共用同一個常數**，
不會有一邊被改掉另一邊沒跟上。

> 驗證方式：我把秒數暫時調成 3 秒重建、截圖確認提亮放大與壓暗都對，**看完立刻改回 0.35**。
> 進版的是 0.35。

---

## 四、圖示系統：對應層完成，**卡在資產**

### 已經做完的

| 項目 | 說明 |
|---|---|
| 優先序取吃法 | `鍋物 > 肉食 > 麵食 > 飯食 > 麵包餅皮 > 小吃 > 湯的 > 輕食`，取第一個命中 |
| fallback | 沒有吃法標籤 → 中性餐具圖示。涵蓋自訂料理、店家、以及哪天資料又漏標 |
| 不改新增表單 | 照規格八-5，**沒有**把吃法改成必選 |
| 檔名規則（工程訂） | `icon-<維度>-<英文值>`，全小寫 kebab-case。轉盤這 9 個是 `icon-form-*` |
| 單色 template | 取用時一律 `.alwaysTemplate`，顏色跟著格子的文字色走 |
| 缺件檢查 | DEBUG 開 App 就列出缺哪幾張 |
| 測試 | 8 支，含「內建 50 道每一道都推得出圖示」「優先序涵蓋全部八個吃法沒漏沒重」 |

### 卡住的

**9 個吃法圖示的 SVG 還沒交。** `Design/` 目前只有規格與提案，沒有任何 `.svg` / `.pdf`
（規格 HTML 裡那 4 個 `<svg>` 是轉盤渲染圖，不是圖示）。

依 PM 的分工：**設計師交原始 SVG → 我轉 PDF、進 `Assets.xcassets`、設定 template 模式**。
圖示長什麼樣不是我該畫的。

### 資產沒到之前，轉盤暫時畫回 emoji

**這不是設計決定，是為了不讓轉盤在圖示交付前變成空白。**

程式已經先去找 `icon-form-*`，找到就用、找不到才畫 emoji。**檔案一放進 asset catalog
這段就自動不再執行，不需要再改任何一行程式。**

需要設計師交的 9 個檔（檔名我已經訂好，照這個命名交進來就能直接用）：

```
icon-form-noodles     麵食
icon-form-rice        飯食
icon-form-soup        湯的
icon-form-hotpot      鍋物
icon-form-snack       小吃
icon-form-bread       麵包餅皮
icon-form-light-meal  輕食
icon-form-meat        肉食
icon-form-neutral     中性 fallback（餐具）
```

規格：24×24 grid、線寬 1.6、圓端圓角、**單色不自帶顏色**（規格八-3a）。

> 菜系角標那 10 個是 S3／S4 才用得到（只在卡片與結果頁），這輪不急。

---

## 五、驗證

| 項目 | 結果 |
|---|---|
| App 建置 | ✅ |
| 單元測試 | ✅ **31 個全過**（S1 的 18 + 這輪新增 13） |
| 12 格滿載實機 | ✅ 零截斷，含 7 字最長菜名 |
| 中選轉場實機 | ✅ 提亮放大 + 其餘壓暗 |
| 深色下的 12 格 | ⚠️ **這次沒取到截圖**，模擬器 appearance 切換沒吃到。S1 已用同一支 `WheelView` 驗過深色盤色與全墨字，這輪改的斷行與轉場都跟 colorScheme 無關 |

### 新增的 13 個測試

**排版（6 支）**，最重要的是「斷行永遠不吃掉任何一個字」—— 拿內建 50 道全部走一遍，
比對 `lines.joined() == name`。截斷是那種改版時很容易悄悄長回來的東西。

**圖示（7 支）**，包含「優先序涵蓋全部八個吃法，沒漏也沒重」：漏一個的話那個吃法的菜
會靜靜落到中性圖示，畫面上看不出是漏了還是設計如此。

### 八條產品規則

**行為零變化。** 這輪動的是畫法與一個 0.35 秒的時序。`FoodPicker`、`FilterBar`、
`FoodEditorView` 一行都沒改。

---

## 六、看到但沒有順手改的

| 東西 | 為什麼沒動 |
|---|---|
| 轉盤白色分隔線與外圈（`.white.opacity(0.75)` / `0.9`） | 規格仍未定義。深色下外圈偏亮 |
| 按鈕、指針、中心鈕仍用系統 accent，不是「醬」 | token 已備好，套用是 S3–S5 |
| 結果頁還是淺色 | 深色只到 `ResultSheet`，那是 S3 |

---

## 七、Commit 計畫（**等 PM 同意才執行**）

```
FoodRotate/Models/WheelLabel.swift        （新增：斷行規則與各格數尺寸）
FoodRotate/Core/FoodIcon.swift            （新增：吃法優先序與 fallback）
FoodRotate/Services/FoodIconAssets.swift  （新增：資產存取與 DEBUG 缺件檢查）
FoodRotate/Views/WheelView.swift          （改：兩行排版、圖示、中選轉場）
FoodRotate/Views/RotateView.swift         （改：傳 winnerIndex、結果頁延後 0.35 秒）
FoodRotate/FoodRotateApp.swift            （改：DEBUG 列出缺件）
FoodRotateTests/WheelLabelTests.swift     （新增）
FoodRotateTests/FoodIconTests.swift       （新增）
```

訊息草稿：

```
S2：轉盤兩行排版、圖示對應層、中選轉場

零截斷：四字以內一行、五字以上兩行，永遠不砍字。舊做法超過五字就補省略號，
「西班牙海鮮燉飯」變成「西班牙海…」，轉盤上看不出那是什麼。12 格滿載
配最長的七字菜名實機驗過。

圖示只表達類型不表達身分——身分歸兩行全名。多個吃法時依優先序取，
鍋物 > 肉食 > 麵食 > 飯食 > 麵包餅皮 > 小吃 > 輕食，湯的排在後面是因為
它講的是狀態不是主體。沒有吃法標籤的（自訂料理、店家）落到中性圖示，
不改新增表單的必填規則。

9 個 SVG 還沒交，找不到資產時暫時畫回 emoji，免得轉盤變空白；檔案一進
asset catalog 那段就自動失效。缺哪幾張 DEBUG 會列出來。

中選那格連同它的字整個重畫再放大，其餘壓暗。結果頁跟著延後 0.35 秒——
原本同一瞬間開，全螢幕 sheet 會把轉場整個蓋掉。
```

---

## 八、還在等的

- **9 個吃法圖示的 SVG** —— 這是 S2 唯一的缺口，檔名規則已列在第四節
- **規格第七節「兩行行序要翻」那句話** —— 實作上翻了才會壞，請設計師確認並修掉
- **結果頁延後 0.35 秒** —— 請 PM 確認這個時序改動

# Food Rotate QC 稽核報告

**檢查日期**：2026-08-19
**角色**：QC／QA
**基準**：`cfa3085`（S6 收批後）
**檢查範圍**：全部 54 支 Swift 原始碼、既有測試、全新 derivedData 建置、上架合規檔案、
`PROJECT_STATUS.md` 與各角色文件的一致性
**修改界線**：本次**有動程式碼**（PM 已核可修 P1 三項）。動到的是
`FoodRotate/Views/RotateView.swift` 一個檔案，加上一支新測試檔。
`FoodPicker.swift` 一行未動，八條產品規則行為零變化。

---

## 一、給專案經理的結論

先講可以放心的部分：**S6 那批修掉的競態沒有回歸**。

| 驗證動作 | 結果 |
|---|---|
| `xcodebuild test`（iPhone 17 Pro 模擬器） | 稽核前 **80 tests / 16 suites 全過** |
| `xcodebuild build`（全新 derivedData，非增量） | **BUILD SUCCEEDED，Swift 警告 0** |

但**逐行讀原始碼找到六項新缺陷**，既有的 80 支測試一支都沒有攔到。原因不是測試寫得不好，
是它們問的問題不一樣：

> **S6 那批問的是「這個結果屬於哪一次請求」（時機）。
> 這一批問的是「模式換了，上一個模式留下來的狀態誰負責清」（歸屬）。**

六項裡有三項是 P1，而且**三項的症狀都是畫面說出不是事實的話**——
這正是這個專案一路在守的那條線（不假裝知道營業時間、不靜默退回、
不把服務故障說成「附近沒有店」）。其中一項更嚴重：它會**把該說的話蓋掉**。

### 這一批做了什麼

| # | 嚴重度 | 一句話 | 狀態 |
|---|---|---|---|
| A | **P1** | 切回「吃什麼」，「附近沒有『歐陸』的店」還掛著，而且**壓掉了真正的放寬提示** | ✅ 已修 |
| B | **P1** | 從歷史還原時模式不跟著切，錯誤卡片也不收 | ✅ 已修 |
| C | **P1** | 「去哪吃」模式調大格數，轉盤被換成菜色 | ✅ 已修 |
| D | P2 | 設定頁改格數繞過 `RotateViewModel`，補格與重設都不會跑 | ⏸ 方向已裁示，**等 PM 排批** |
| E | P3 | `MyListView` 多選刪除索引錯位（現在打不到，加編輯鈕就會） | ⏸ 等 PM 排批 |
| F | P3 | 候選池被排空時，訊息說「忌口把選項篩掉了」——可能一個忌口都沒選 | ⏸ 等 PM 排批 |
| — | **P1** | `PrivacyInfo.xcprivacy` 仍然不存在（上架硬門檻） | ⏸ **仍卡 kuoyo**，未動 |

修完之後：**86 tests / 17 suites 全過**（新增 6 支），全新 derivedData 建置 **Swift 警告 0**。

### 建議判定

| 面向 | 判定 | 說明 |
|---|---|---|
| 核心離線抽選邏輯 | 綠 | `FoodPicker` 本次未動，既有測試全過 |
| 建置與測試 | 綠 | 86/86，全新建置零警告 |
| 轉盤／搜尋的非同步邊界 | 綠 | S6 那批的防線有效，本次未發現回歸 |
| **模式切換的狀態歸屬** | **黃 → 綠** | 三項 P1 本批已修並補上會紅的測試 |
| 格數這個設定的入口 | **黃** | D 未修：兩條寫入路徑，只有一條維護不變量 |
| App Store 準備 | **黃紅** | 仍然沒有 `PrivacyInfo.xcprivacy` |
| 文件治理 | 綠 | `PROJECT_STATUS.md` 是唯一入口且未過期，這一輪要記得更新 |

---

## 二、確認到的優點

不是客套話，是這一輪稽核實際靠它們省下時間的東西：

1. **`Generation` 號碼牌只有一份，兩邊共用。** 這是上一輪最好的決定。
   這次讀「去哪吃」那條路徑時，`isCurrentSearch` 一眼就能確認邊界在哪，
   不必去比對四套各自實作的取消邏輯。
2. **決定都寫下了前提。** `WheelSpinner.finish` 為什麼要 `run` 而不只是 `isSpinning`、
   `HistoryStorage.save` 為什麼不做整合測試 —— 這些註解讓稽核可以直接判斷
   「這是決定還是遺漏」，不用重跑一次推理。
3. **既有測試測的是行為不是實作。** `WheelSpinnerRaceTests` 的閘門形狀直接被本次
   新測試沿用，六支新測試不到一小時就寫得出來。
4. **`clearNotices()` 這種缺陷之所以能一眼看出來**，是因為 `statusArea` 的
   五種提示互斥順序被明確註解成「既有優先序，不要動」——
   優先序寫下來了，才看得出「排在前面的那個殘留會蓋掉後面的」。

---

## 三、問題清單

嚴重度定義沿用 `QC專案健康檢查報告-2026-08-16.md` 第三節。

### A（P1，已修）退回一般餐廳的提示跨模式殘留，並蓋掉「已放寬」

**位置**：`FoodRotate/Views/RotateView.swift` — `didFallBackToGeneric`／`searchedCuisines`
的宣告、`pick()`、`findRestaurants()`、`statusArea` 的 `else if` 鏈。

**證據**：`didFallBackToGeneric` 與 `searchedCuisines` **只在 `findRestaurants()` 裡歸零**。
`pick()` 清了 `errorMessage`、`isLoading`、`relaxedDimensions`、`isOverConstrained`，
唯獨沒有碰這兩個。而 `source` 的 `didSet` 切到 `.dishes` 時走的正是 `load()` → `pick()`。

**重現**：
1. 「去哪吃」→ 選「歐陸」。
2. 附近沒有歐陸的店，退回一般餐廳，提示出現。
3. 切到「吃什麼」。
4. 提示還在，寫著「附近沒有『歐陸』的店，改列一般餐廳」——這裡根本沒有在找店。

**為什麼是 P1 而不是 P2**：`statusArea` 是一條互斥的 `else if` 鏈，
`didFallBackToGeneric` 排在 `relaxedDimensions` **前面**。所以殘留的那句話不只是雜訊，
它會讓「不夠 8 道，已放寬 菜系」**整句不顯示** —— 使用者拿到不完全符合的東西而畫面沒說。
那是產品規則第三條失效，跟當初 `didFallBackToGeneric` 這個欄位存在的理由是同一條。

**改法**：新增 `clearNotices()`，把那條提示鏈的**五個來源欄位一次歸零**，
`pick()` 與 `findRestaurants()` 兩條路都先走它。
歸零只有一個地方，以後再加第六個提示欄位時不會又漏掉一條路 —— 那正是這個缺陷的成因。

**已加測試**：
- 「退回一般餐廳之後切回『吃什麼』，那句提示不得留在畫面上」
- 「退回的提示不得把『已放寬』那一句蓋掉」（連放寬本身也一起斷言，
  否則前提不成立時這支測試會假綠）

---

### B（P1，已修）從歷史還原，模式與錯誤卡片留在上一個模式

**位置**：`FoodRotate/Views/RotateView.swift` — `restore(_:)`。

**證據**：`restore` 設了 `allItems`、`relaxedDimensions`、`isOverConstrained`、`winner`，
但**沒有設 `source`，也沒有清 `errorMessage` 與 `didFallBackToGeneric`**。

**重現**：
1. 開飛航模式 →「去哪吃」→ 定位／搜尋失敗，出現「找不到附近的店」。
2. 歷史頁點一筆菜色紀錄還原（`canRestore` 保證它一定是菜色）。
3. 回到轉盤：盤上是還原的菜，上面壓著一張「找不到附近的店…改用『吃什麼』模式」的卡片，
   而使用者**已經在**菜色清單上了；切換器仍指著「去哪吃」。
4. 接著動任何一個標籤 → `onChange` → `load()` → `findRestaurants()`，
   剛還原的清單當場被沖掉。

**改法**：`restore` 先切 `source = .dishes`、再 `clearNotices()`、**最後**才指派 `allItems`。

**順序在這裡是正確性不是風格**：`source` 的 `didSet` 會 `abandonSearch()`、
清空 `allItems` 再 `load()`。先放清單的話，那個 `didSet` 會立刻把剛還原的東西沖掉。
這一點已經寫成程式碼裡的註解 —— 不寫的話，下一個人整理欄位順序時會直接改壞，
而症狀（「還原沒反應」）跟原因（賦值順序）之間看不出關聯。

**已加測試**：
- 「從歷史還原一組菜色，模式要跟著回到『吃什麼』，錯誤卡片要收掉」
- 「還原之後改條件，重抽的是菜不是店」

> **這一支踩了一個坑，記在這裡。** 它第一版是綠的 —— 但綠的理由跟它要驗的事情無關：
> 用「會失敗的搜尋」當前提，失敗會把 `allItems` 清成空陣列，
> 而空陣列的 `allSatisfy` 恆真。第二版改成「找得到店」的搜尋，仍然綠 ——
> 因為 `waitUntil { !allItems.isEmpty }` 在還原後立刻成立，斷言跑在搜尋落地之前。
> 第三版改成等 `!isLoading`，才紅。**假綠的測試比沒有測試危險**，
> 所以每一支都先確認在修之前會紅才算數。

---

### C（P1，已修）「去哪吃」模式調大格數，轉盤被換成菜色

**位置**：`FoodRotate/Views/RotateView.swift` — `wheelSlots` 的 setter。

**證據**：補格那一行是無條件的 `pick()`，不看 `source`。
菜色模式下 `allItems.count` 永遠等於 `wheelSlots`，所以平常打不到；
**但「去哪吃」模式的 `allItems` 是搜到幾家算幾家**。

**重現**：「去哪吃」，附近只找到 5 家（郊區，或選了冷門菜系退回之後仍然少）
→ 把格數從 4 調到 8 → `5 < 8` 成立 → `pick()` → 轉盤上突然出現 8 道菜，
模式切換器還停在「去哪吃」，而 `foundPlaces` 裡還留著上一批店的座標。

**改法**：`if source == .dishes, allItems.count < newValue { pick() }`。

**刻意不改成 `load()`**：那會變成每調一次格數就重新定位加搜尋一次。
店家模式下「湊不滿」是事實，不是可以再抽一次修正的東西 —— 附近有幾家就是幾家，
那件事本來就由既有的 `isShortOfSlots` 老實說出來。

**已加測試**：
- 「『去哪吃』模式把格數調大，轉盤上不得冒出菜色」
- 「『吃什麼』模式把格數調大，照舊會補滿」——**反面那一支是必要的**：
  加模式判斷很容易把原本對的行為一起關掉，沒有它，C 的修法可以用「乾脆不補格」蒙混過去。

---

### D（P2，**未修，方向已裁示**）設定頁改格數繞過單一入口

**位置**：`FoodRotate/Views/SettingsView.swift` 的 `Picker("轉盤格數", selection: $settings.wheelSlots)`
直接綁 `AppSettings`，繞過 `RotateViewModel.wheelSlots` 的 `winner = nil` /
`spinner.reset()` / 補格。

**後果三層**：

1. 8→12 之後轉盤仍是 8 格，狀態列跳出「只湊得出 8 格，可以少選條件或調低格數」——
   條件根本沒問題，**那句話是假的**。
2. 12→4 之後 winner 可能已經不在 `items` 裡，中選高亮與 `restingAngle` 對不上。
3. **轉動中**切到設定頁改格數：`spinner` 手上的 `segmentCount` 跟 `items.count`
   不再是同一個數 —— 指針停的那一格跟結果不是同一格；而若 winner 落在被截掉的那段，
   `spin` 的 guard 直接 return：**轉盤轉完，什麼都沒發生，也沒有任何說明。**
   （不會崩潰 —— S6 的兩層防線擋住了。擋住的是崩潰，不是「什麼都沒發生」。）

**已裁示的方向（kuoyo，2026-08-19）：收成單一入口。**
把「清 winner、reset spinner、不夠就補格」搬到格數變更的唯一路徑上，
設定頁與轉盤頁走同一條 —— 跟這個專案既有的「同一條規則只能有一個來源」是同一條原則
（`FoodIcon` 的圖示對應、`HistoryStorage` 的 `try?` 收斂都是這樣做的）。

**本批不動它的理由**：它要碰 `SettingsView` 與 `RotateViewModel` 的介面，
值得自己一批、自己一次驗收；夾帶進來會讓這一批的驗收失去意義。

**一起處理**：轉動中不該讓格數可改（`FilterBar` 的 `slotPicker` 與設定頁都沒有擋）。

---

### E（P3，未修）`MyListView` 多選刪除索引錯位

**位置**：`FoodRotate/Views/FoodEditorView.swift` 的 `MyListView.onDelete`。

```swift
.onDelete { offsets in
    for index in offsets { store.exclude(store.customItems[index]) }
}
```

`exclude` 當場改動 `customItems`，第二個 index 就指到別人，或直接越界。

**現在打不到**：這一頁沒有 `EditButton`，左滑刪除的 `offsets` 實務上只有一個。
但它跟 `PROJECT_STATUS.md` 已經記著的 `FoodEditorView` 的 `ForEach(points.indices)`
是同一類東西：**現況剛好安全，加一顆按鈕就會出事。**

**改法（一行）**：先把要刪的取出來，再刪 ——
`offsets.map { store.customItems[$0] }.forEach(store.exclude)`。

---

### F（P3，未修）候選池被排空時，訊息指錯地方

**位置**：`FoodRotate/Core/FoodPicker.swift` 與 `RotateView.swift` 的 `emptyHint`。

`isOverConstrained` 是用 `items.isEmpty` 算出來的，所以「候選池本身是空的」
（使用者把 50 道全部「以後都不要」）跟「忌口把選項篩光了」會走到同一個分支，
畫面說「忌口條件把所有選項都篩掉了…請把其中一個忌口取消再試」——
但使用者可能一個忌口都沒選，而且照那句話做也救不回來。

**改法**：兩種「空」要分開。後者的出口在設定頁的「我的清單」，訊息要指向那裡。
**注意這一項會碰到 `FoodPicker`**，那個檔案 S1 至今一行未改，動它要 PM 明確點頭。

---

### 仍然掛著：`PrivacyInfo.xcprivacy`（P1，上架硬門檻）

`find . -iname "*.xcprivacy"` → **0 筆**。與 2026-08-16 那份報告的狀態相同，
仍然卡在「kuoyo 要不要送 App Store」這個決定上。
App 有用 `UserDefaults`（`AppSettings`、`CustomFoodStore` 兩處），
送審需要宣告 required reason API。**這不是工程可以自己決定的事，不猜、不動。**

---

## 四、本次執行了什麼

### 已執行

- `xcodebuild test -scheme FoodRotate -destination 'platform=iOS Simulator,id=…'`（iPhone 17 Pro）
  - 稽核前：`80 tests in 16 suites passed`
  - 修完後：`86 tests in 17 suites passed`
- `xcodebuild build -derivedDataPath <全新目錄>`
  - **刻意不用增量建置** —— 增量沒噴不等於沒有警告。
  - 結果：`BUILD SUCCEEDED`，Swift 警告 **0**
    （只有一則 `appintentsmetadataprocessor` 的 metadata 提示，與程式碼無關）。
- 全部 54 支 Swift 檔逐行讀過。
- `find . -iname "*.xcprivacy"` → 0 筆。
- `Info.plist`／`FoodRotateWidget-Info.plist` 檢查：
  定位用途說明、`NSSupportsLiveActivities`、`LSApplicationQueriesSchemes` 都在。

### 本次未能直接證明

- **沒有真機。** 定位品質、電話撥出、Apple／Google 地圖跳轉、Live Activity 在
  鎖定畫面上的實際樣子，這一輪都沒有驗 —— 三項 P1 的實機重現步驟寫在上面，
  建議併進 `PM/實機回歸檢查表.md` 走一次。
- **沒有 archive／App Store validation**，所以 privacy manifest 那一項仍然是
  「依官方規則確認的缺口」，不是上傳驗證過的結果。
- D 的第三層後果（轉動中改格數導致指針與結果不同格）是依控制流可確定的，
  但**沒有錄影證明** —— 它要一邊轉一邊切分頁，模擬器上不好拍。

---

## 五、順手記一筆（不是缺陷）

修 A 的時候發現 `findRestaurants()` 的文件註解原本**黏在 `abandonSearch()` 頭上**
（兩段註解中間少了一個空行，Xcode 把它算給了下一個宣告）。
新方法插進那個位置時把問題放大了，所以一併把註解還給 `findRestaurants()`。
**沒有改任何一行程式碼行為**，純粹是註解歸位 —— 在此記明，避免 PM 對 diff 時困惑。

---

## 六、下一批的建議順序

1. **D（P2）** —— 方向已裁示，是這幾項裡唯一還會讓畫面說假話的（「只湊得出 8 格」）。
   併著「轉動中不給改格數」一起做。
2. **P1-5 privacy manifest** —— 不是工程排得動的，等 kuoyo。
3. **E、F（P3）** —— 兩個都是一行到十行的改動，可以併進任何一批。
   F 要碰 `FoodPicker`，需要 PM 明確點頭。

**這一輪之後的準確狀態**：核心邏輯與非同步邊界是綠的，
**模式切換的狀態歸屬本批補上防線**，剩下的是一個設定入口的重複寫入路徑（D）
與一個不在工程手上的上架決定（P1-5）。

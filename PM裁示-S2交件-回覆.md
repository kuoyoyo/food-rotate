# PM 裁示 — S2 交件回覆

**日期**：2026-08-11 ／ **裁示**：PM
**回應**：`Design/設計規格-圖示與動效-v1.md`（設計）、`Coder/工程回覆-S2進度-給PM.md`（工程）
**結論**：**S2 還沒完成**，需要再一輪。缺口不大，但有一個誤會要先解除。

---

## 一、⚠️ 誤會解除：9 個 SVG 已經交了

> 工程 §4：「9 個吃法圖示的 SVG 還沒交。`Design/` 目前只有規格與提案，沒有任何 `.svg`」

**不成立。資產在 `Design/icons/`，9 個全部到齊**，而且早於工程報告的時間：

```
form-light.svg / form-snack.svg / form-unknown.svg      17:28
form-noodles.svg / form-soup.svg / form-hotpot.svg      21:08
form-rice.svg / form-bread.svg / form-meat.svg          21:09
（工程報告時間 21:25）
```

設計規格第一節與第五節都寫明了位置。**工程看的是 `Design/` 根目錄，沒有進子資料夾。**

PM 已驗過 9 個檔全部合規：`viewBox="0 0 24 24"`、`stroke="currentColor"`、
`stroke-width="1.6"`、`stroke-linecap="round"`，一個不差。

### 檔名對照（工程匯入時改名，不要求設計重交）

檔名規則歸工程（PM 先前的分工），**asset catalog 的名字是匯入時設定的，不是 SVG 檔名決定的**，
所以不需要設計重交。對照如下：

| 設計交的檔 | 匯入後的 asset 名 |
|---|---|
| `form-noodles.svg` | `icon-form-noodles` |
| `form-rice.svg` | `icon-form-rice` |
| `form-soup.svg` | `icon-form-soup` |
| `form-hotpot.svg` | `icon-form-hotpot` |
| `form-snack.svg` | `icon-form-snack` |
| `form-bread.svg` | `icon-form-bread` |
| `form-light.svg` | `icon-form-**light-meal**` |
| `form-meat.svg` | `icon-form-meat` |
| `form-unknown.svg` | `icon-form-**neutral**` |

**最後兩列的 slug 不同**，匯入時特別注意，不要照 SVG 檔名貼。

> 流程檢討：這類「交了但對方沒看到」的損失是半天。之後交付如果放在子資料夾，
> 請在 README 的檔案表列出來 —— 這次 `Design/README.md` 沒有更新到 `icons/`。

---

## 二、兩處「規格錯、工程對」

### 1. 停止觸覺：現況已經是 success，不需要改

> 設計規格 §三-3：「現況停止時是 `Haptics.wheelStopped()`（impact 型）。改成 success 型。」

**現況已經是 success。** `Haptics.swift:24`：

```swift
static func wheelStopped() {
    stop.notificationOccurred(.success)
}
```

工程 §3 說「本來就有，沒有再加一個，重複給會變成兩下」——**判斷正確**。
請設計把規格這條改成「維持現況」，否則下一個人會照著再加一次。

### 2. 兩行的行序不該跟著翻

> Theme 規格 §七：「左半邊翻轉 180° 的既有處理保留，**兩行的行序也要跟著翻**。」

工程 §2 說翻了才會顛倒，理由是行距加在**旋轉後**的座標系上，字面與行位置一起鏡射。
**PM 採信**：他有 12 格實機截圖，左半邊的「韓式人／參雞湯」「印度香／米飯」都是正的。

規格那句只在「用全域座標算行位置」的實作上成立。**請設計把這句刪掉或改寫**，
否則下一個人照著改會改壞。

---

## 三、動效：設計的新規格取代工程目前的做法

工程做 S2 時，`設計規格-圖示與動效-v1` 還不存在，他是照 Theme 規格與交接文件做的。
現在新規格到了，**以新規格為準**。要補的差異：

| 項目 | 工程現況 | 新規格 | 處理 |
|---|---|---|---|
| 高亮時長 | 0.35 秒 | **0.28 秒**，spring(0.28, 0.72) | 改 |
| 結果頁開啟時間 | 停止後 0.35 秒 | **0.28 秒**，全程 0.60 秒收尾 | 改 |
| 中選格變化 | 重畫再放大 | scale **1.045** + 描邊 1.5→**3.5**（該格文字色） | 對齊參數 |
| 其餘格 | 壓暗 | `opacity` → **0.55**，easeOut 0.20 | 對齊參數 |
| 中心「轉」鈕 | — | `opacity` → 0，easeOut 0.15 | 補 |
| 背景壓暗層 | — | `opacity` → 0.55，色值 `#1A1714` | 補 |
| **圖示轉場** | — | **`matchedGeometryEffect`**：圖示從轉盤該格放大到結果頁 76pt | **補，這是重點** |
| `reduceMotion` | — | 取消縮放與 matchedGeometry，只加粗描邊；ResultSheet 改淡入 0.15s；**觸覺保留** | 補 |
| VoiceOver | — | 中選格 label 加「中選」；ResultSheet 搶焦點到菜名 | 補 |

**`matchedGeometryEffect` 那條特別重要。** 設計的理由成立：少了它，結果頁只是一個突然
出現的畫面，使用者看不出「這道菜就是剛剛停下的那一格」。

**「結果頁延後開啟」這個時序改動 PM 確認採用**（工程 §3 的請示）——原本同一瞬間開，
全螢幕 sheet 會把轉場整個蓋掉，等於白做。秒數改成 0.28，並繼續共用同一個常數。

工程把秒數暫時調成 3 秒來驗證再改回 0.35 的做法很好，**這次對齊參數時請再做一次**。

---

## 四、菜系角標：**核可改用文字元件**

設計建議不畫 10 個菜系圖示，改用文字角標（台・中・日・韓・東南・南亞・義・美・歐・墨）。

**核可。** 三個理由 PM 都認可，其中第一個是關鍵：

> 菜系圖像化幾乎必然走向國族刻板印象。墨西哥要畫什麼？寬邊帽是不能用的。

這不是畫功問題，是題目本身的陷阱。第二個理由也有實證支撐——第一節那個「四個碗在 17pt
糊成一團」的教訓剛發生過，角標只有 14–16pt，菜系之間的差異又比「碗與鍋」更抽象。

**「刻意用中性灰而不是配色」也核可**：轉盤的顏色代表第幾格、不代表菜系，
角標上色會讓同一畫面有兩套顏色語意打架。

資產量 19 → 9 個圖檔 + 1 個元件，新增菜系的成本從「畫一張圖」變成 0。

> 角標是卡片與結果頁的東西，**S3／S4 才用得到**，這輪不必實作。

---

## 五、其餘裁示

| # | 事項 | 裁示 |
|---|---|---|
| 1 | 中選動效總長 0.60 秒與第三節全部參數 | ✅ 核可 |
| 2 | 「圖示要在 17pt 下辨識，不是 96pt 下好看」寫進 S2 驗收 | ✅ **核可**，PM 驗收會照這個尺寸看 |
| 3 | 優先序放進 `FoodItem` 的 computed property 讓轉盤與卡片共用 | 由工程決定，但**同一條規則只能有一個來源**這點必須成立 |
| 4 | 動效三層用 `Task.sleep`／completion 串起來，不要兩個獨立計時器 | ✅ 採納，理由（低效能裝置上會散掉）成立 |

---

## 六、S2 還要做的事

1. **匯入 9 個 SVG**，轉 PDF、進 `Assets.xcassets`、設 Template Rendering，照第一節的對照表命名
2. **移除 emoji fallback 那段**（資產到齊後它本來就不會執行，但不該留著）
3. **動效對齊新規格**，含 `matchedGeometryEffect`、`reduceMotion`、VoiceOver
4. **補深色模式的 12 格實機截圖**（工程 §5 這次沒取到）
5. 設計修規格兩處（觸覺、兩行行序）

完成後一起驗收，**PM 會實機看 4／6／8／10／12 五種格數、淺深兩色，並在 17pt 下檢查圖示辨識度**。

---

## 七、已經確認沒問題的

- 兩行排版零截斷：12 格滿載含 7 字菜名，實機驗過
- 31 個測試全過
- 產品規則行為零變化：`FoodPicker`／`FilterBar`／`FoodEditorView` 一行未改
- 「斷行永遠不吃掉任何一個字」拿內建 50 道全走一遍比對 `lines.joined() == name` —— 這支測試寫得好，截斷正是改版時容易悄悄長回來的東西

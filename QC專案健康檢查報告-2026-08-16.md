# Food Rotate 專案健康檢查報告

**檢查日期**：2026-08-16  
**角色**：QC（唯讀稽核）  
**檢查範圍**：完整專案、現有產品／設計／PM 文件、`main...HEAD` 分支差異、單元測試、建置設定與上架合規準備  
**修改界線**：本次沒有修改任何 Swift、JSON、plist、Xcode 專案檔或既有文件；唯一新增檔案就是本報告。

---

## 一、給專案經理的結論

專案的核心方向是正確的，而且基礎品質高於一般原型：架構分層清楚、沒有第三方套件、50 筆內建資料有自動稽核、重要產品規則有單元測試、設計決策與驗收紀錄非常完整。2026-08-16 實際執行測試，**43 個測試、7 個 suites 全數通過**。

但目前還不建議直接視為「可安心送審／功能完全收尾」。本次確認到：

- **1 項可能造成 App 崩潰的高風險轉盤競態**。
- **3 項「去哪吃」與歷史還原的主要功能錯誤**。
- **1 項 App Store 隱私 manifest 缺口**。
- 若干資料持久化、文案真實性、測試策略與文件治理問題。

建議先完成 P0／P1，再安排一次針對「快速連續操作、搜尋取消、餐廳歷史還原、冷啟動持久化、上架 archive」的回歸驗收。

### 建議判定

| 面向 | 判定 | 說明 |
|---|---|---|
| 核心離線抽選邏輯 | 綠 | `FoodPicker`、資料稽核、圖示與排版測試完整，43 tests 全過 |
| 一般編譯／測試 | 綠 | 模擬器測試成功，未發現編譯錯誤或警告造成的阻擋 |
| 轉盤快速操作穩定性 | 紅 | 舊的完成 Task 未被取消，可提前結束新轉盤並使用舊索引 |
| 附近餐廳非同步狀態 | 紅 | 切模式／改條件時可能接收過期結果；找不到時的 fallback 路徑不完整 |
| 歷史還原 | 黃紅 | 料理可還原；餐廳紀錄缺來源與導航資料，還原後操作可能失效 |
| App Store 準備 | 黃紅 | 使用 `UserDefaults`，但專案內沒有 `PrivacyInfo.xcprivacy` |
| 視覺與規格一致性 | 綠偏黃 | 文件顯示多輪實機驗收；但沒有可重現的 UI／snapshot 自動化證據 |
| 文件治理 | 黃 | 根 README 與 `Coder/README.md` 的進度、待辦已落後實際驗收狀態 |

---

## 二、確認到的優點

### 1. 產品原則明確，而且有落到程式與測試

- 忌口是硬條件，`FoodPicker` 先篩限制，再放寬軟條件。
- 放寬採「保留完全符合項目，再向下補」而不是整批重抽。
- 「去哪吃」明確承認 MapKit 是關鍵字搜尋，不假裝是菜系分類。
- MapKit 沒有營業狀態就不顯示假的資訊，改提供電話與地圖入口。
- 產品規則、設計判斷與工程取捨都有文件來源，接手者能理解「為什麼」。

### 2. 架構邊界合理

- `Core/` 只依賴 Foundation，抽樣與資料模型沒有綁死 SwiftUI。
- UI、服務、設定、資料模型分層清楚。
- `foods.json` 可移植，符合未來 Android 共用資料的方向。
- 外部套件為零，供應鏈與升級風險低。

### 3. 資料品質有基本防線

- 50 道料理的 ID 沒有重複，名稱也沒有重複。
- `FoodDataAudit` 會檢查重複 ID、菜系／吃法缺標籤與過期豁免。
- 實際 JSON 抽查未發現優缺點數量、空名稱、空 emoji 或 25 字限制異常。

### 4. 現有測試在它覆蓋的範圍內很有價值

- FoodPicker 的硬限制與逐層放寬有重複洗牌測試，避免隨機僥倖通過。
- 設計 token 有對比度、色序、重複色與換算測試。
- 輪盤文字排版、圖示對應、資料 audit、搜尋詞都有針對性測試。
- 2026-08-16 實跑結果：**43 tests / 7 suites passed**。

### 5. Swift 6 與非同步設計的基本意識良好

- 使用 `@MainActor` 收斂 UI 狀態。
- 定位採 async sequence，並對無限更新流設置 30 秒上限。
- Live Activity 不保存非 Sendable 的 `Activity` 物件，而保存 ID 再查回。
- 取消搜尋 Task、定位快取、背景執行時間與 stale activity 都有考慮。

### 6. 設計改版的變更控制不錯

- Bugbot 對 `main...HEAD` 的 10 個檔案（+757/−79）沒有找到高／中嚴重度的分支差異 bug。
- `git diff --check main...HEAD` 無 whitespace 問題。
- 分支以樣式與 token 套用為主，既有互動與持久化邏輯大多沒有被夾帶修改。
- 字體使用語意 text styles，保留 Dynamic Type；固定淺／深畫面也有明確處理 inherited color scheme。

---

## 三、問題清單與具體改善方式

嚴重度定義：

- **P0**：可能崩潰、資料嚴重錯亂或核心流程不可用，應立即處理。
- **P1**：主要功能會給錯結果、無法完成或影響上架，應在下一版前處理。
- **P2**：不一定阻擋發布，但會造成誤導、資料不一致或難以維護。
- **P3**：品質與流程改善，建議納入後續工程基線。

### P0-1：舊轉盤完成 Task 沒有被取消，可能提前結束新轉盤並以舊索引崩潰

**位置**：

- `FoodRotate/Views/WheelView.swift:19`
- `FoodRotate/Views/WheelView.swift:40`
- `FoodRotate/Views/WheelView.swift:56`
- `FoodRotate/Views/RotateView.swift:228`
- `FoodRotate/Views/RotateView.swift:230`

**證據**：`WheelSpinner` 只保存並取消 `tickTask`；真正等待 4.2 秒後呼叫 `finish` 的 Task 沒有被保存。`reset()` 因此取消不了它。`finish()` 只檢查全域的 `isSpinning`，無法分辨目前轉的是舊一輪還是新一輪。

**可重現流程**：

1. 開始轉 A。
2. A 尚未結束時改條件或換一組；`pick()` 會呼叫 `spinner.reset()`。
3. 立刻開始轉 B。
4. A 的舊 Task 到點後看到 B 的 `isSpinning == true`，於是把 B 提前結束，並執行 A 的 `onFinish`。
5. A 的 closure 用舊 `index` 讀取現在的 `self.items[index]`；若新清單較短，會 array out of range 崩潰；即使沒崩潰，也可能選錯菜並寫錯歷史。

**具體改善**：

- 為完成等待新增可取消的 `finishTask`，在每次 `spin` 前及 `reset()` 中取消。
- 再加一個每輪唯一的 generation/token；醒來後必須確認 token 仍是目前這輪。
- `RotateViewModel.spin` 不要只捕捉索引；起轉時同時捕捉選中的 `FoodItem.id` 或完整 item，完成時再驗證目前清單仍包含同一 ID。
- 轉動中可考慮暫時停用會改變候選／格數的控制，降低狀態組合，但這不能取代 Task 身分驗證。

**必加測試**：reset A → 啟動 B → A 到點不應影響 B；新清單較短時不得崩潰；只有 B 的 completion 可執行一次。

### P1-1：餐廳搜尋完成後可覆蓋已切回「吃什麼」的畫面

**位置**：

- `FoodRotate/Views/RotateView.swift:13`
- `FoodRotate/Views/RotateView.swift:125`
- `FoodRotate/Views/RotateView.swift:135`
- `FoodRotate/Views/RotateView.swift:164`
- `FoodRotate/Views/RotateView.swift:188`
- `FoodRotate/Views/RotateView.swift:195`

**證據**：從餐廳切回料理時，`pick()` 只把 `isLoading` 設成 false，沒有呼叫 `nearby.stop()` 或使舊 callback 失效。舊搜尋完成後仍會進入 `apply()`，把餐廳結果寫回 `allItems`。

**影響**：畫面 segmented control 顯示「吃什麼」，但轉盤內容變成餐廳；模式、篩選說明與資料來源互相矛盾。

**具體改善**：

- 切換 source 時先取消舊搜尋並結束對應 Live Activity／background task。
- 為每次搜尋建立 request ID；`apply` 必須同時確認 request ID 與 `source == .restaurants`。
- `apply` 接受結果本身，不要再從共享 `nearby.phase` 讀取可能已被下一個請求改掉的狀態。

**必加測試**：啟動慢速餐廳搜尋 → 切回料理 → 舊搜尋回傳；料理清單不得被覆蓋。

### P1-2：載入中改菜系／距離會被忽略，畫面條件與結果可能不同

**位置**：`FoodRotate/Views/RotateView.swift:164-166`

**證據**：FilterBar 在條件與距離改變時會呼叫 `model.load()`，但 `findRestaurants()` 的 `guard !isLoading else { return }` 直接忽略載入中的新需求。

**影響**：使用者看到新菜系或新距離已被選中，最後收到的卻是舊搜尋結果；這違反專案很重視的「不假裝、放寬要說明」原則。

**具體改善**：

- 新條件到來時取消舊搜尋並啟動新搜尋，而不是忽略。
- 可加 200–300ms debounce，避免連點多個 tag 時對 MapKit 連續發送請求。
- 結果套用前比對 request ID、菜系集合與搜尋半徑快照。

### P1-3：MapKit 的「找不到」拋錯會跳過一般餐廳 fallback；多搜尋詞也會整批失敗

**位置**：

- `FoodRotate/Services/NearbySearch.swift:203`
- `FoodRotate/Services/NearbySearch.swift:209`
- `FoodRotate/Services/NearbySearch.swift:245-248`
- `FoodRotate/Services/NearbySearch.swift:290-306`

**證據**：程式自己的註解已記錄 `MKLocalSearch` 找不到店家時會拋 `placemarkNotFound`，不是一定回空陣列。但一般餐廳 fallback 只在 `places.isEmpty` 時執行；若搜尋直接 throw，會進最外層 catch，完全跳過 fallback。

多詞搜尋也有同一問題。例如「東南亞」會查「泰式」與「越南料理」；第一個已找到結果後，第二個若 throw，整個 `searchPlaces(terms:)` 仍 throw，前面的有效結果全部被丟掉。

**具體改善**：

- 把 `placemarkNotFound`／`directionsNotFound` 正規化成該搜尋詞的空結果。
- 多詞搜尋應逐詞處理：not-found 就 continue；server failure、throttle、decode failure 才決定是否中止整輪。
- 菜系所有詞都無結果後，再執行一般「餐廳」搜尋並設 `didFallBackToGeneric`。
- 新增測試：單詞 not-found 會 fallback；第二詞 not-found 不會抹掉第一詞結果；服務端錯誤不應被誤當沒店。

### P1-4：餐廳歷史紀錄無法完整還原，來源錯置且導航／電話可能無反應

**位置**：

- `FoodRotate/Views/RotateView.swift:285-304`
- `FoodRotate/Models/SpinRecord.swift:10-27`
- `FoodRotate/Views/HistoryView.swift:62-66`

**證據**：所有 source 的 spin 都寫入同一種 `SpinRecord`，但紀錄只保存 FoodItem JSON、條件摘要與 winnerName，沒有保存 `WheelSource`，也沒有保存餐廳的座標／電話／網站。`restore()` 只替換 `allItems`，不切換 source，也不重建 `foundPlaces`。

**影響**：

- 還原餐廳紀錄後，模式可能仍顯示「吃什麼」。
- 餐廳 item 仍帶 `place-` ID，所以 UI 顯示「導航過去」，但 `foundPlaces` 沒資料，按鈕會安靜地沒有反應。
- 若在「去哪吃」模式還原料理紀錄，也會出現相反的來源錯置。

**具體改善（PM 需先選產品策略）**：

1. **完整保存餐廳紀錄**：SpinRecord 增加 source、winnerID 與可還原的 place metadata；做 SwiftData migration。還原時同步 source 與導航資料。
2. **只保存料理轉盤**：餐廳 spin 不寫歷史，並在 UI／文件明確說明。這是較小範圍、但會減少目前暗示的能力。
3. **餐廳歷史只作展示不可還原**：歷史列依 source 決定是否顯示還原圖示，避免提供死按鈕。

不論選哪一種，都應加「料理紀錄／餐廳紀錄／舊版紀錄」三類測試。

### P1-5：缺少 `PrivacyInfo.xcprivacy`，使用 UserDefaults 的送審合規未完成

**位置**：全專案找不到 `PrivacyInfo.xcprivacy`；`AppSettings`、`CustomFoodStore` 與 DEBUG 啟動參數有使用 `UserDefaults`。

**依據**：Apple 將 `UserDefaults` 列為 required reason API；App 自己使用時需在 privacy manifest 的 `NSPrivacyAccessedAPITypes` 描述用途。此專案的偏好資料僅供 App 自己讀寫，應由負責送審的人核對是否適用 Apple 列出的 `CA92.1`，不可直接照抄而不審核。

Apple 官方文件：

- [Privacy manifest files](https://developer.apple.com/documentation/bundleresources/privacy-manifest-files)
- [Describing use of required reason API](https://developer.apple.com/documentation/bundleresources/describing-use-of-required-reason-api)
- [NSPrivacyAccessedAPIType（含 UserDefaults reasons）](https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitype)

**具體改善**：

- 新增合法的 `PrivacyInfo.xcprivacy` 到 App target resources。
- 宣告 UserDefaults 的正確 required reason；同時確認定位資料是否有被 App 或第三方收集，再如實填寫 data collection 區段。
- Archive 後用 Xcode 產生 privacy report，確認主 App 與 extension 的 manifest 合併結果。
- 將「archive + privacy report + App Store validation」加入 release checklist。

### P2-1：距離錯誤文案固定寫十五公里，與實際 1／3／5／10 公里設定矛盾

**位置**：

- `FoodRotate/Services/NearbySearch.swift:218`
- `FoodRotate/Services/NearbySearch.swift:277`

**證據**：實際 `maxDistance` 來自設定，預設 5 公里、上限 10 公里；失敗文案卻固定顯示「十五公里內」。

**改善**：統一透過 `SearchRadius.label(AppSettings.shared.searchRadius)` 產生文案；所有錯誤與 empty state 都只讀同一個距離來源。加入四種半徑的文案測試。

### P2-2：附近店家地圖固定只顯示 2.5 公里，可能看不到合法的遠端結果

**位置**：`FoodRotate/Views/NearbyRestaurantsView.swift:162-170`

**證據**：搜尋可接受 10 公里內結果，但 map camera 永遠是 2,500 × 2,500 公尺，距離較遠的 marker 會落在初始視窗外。

**改善**：用使用者位置加所有結果計算 bounding region，或至少依目前 searchRadius 設定 camera span；結果更新時也應更新一次，而不是只靠 `onAppear`。

### P2-3：自訂料理改名有兩套資料來源，設定頁名稱與實際抽樣名稱可不同

**位置**：

- `FoodRotate/Services/CustomFoodStore.swift:42-56`
- `FoodRotate/Services/CustomFoodStore.swift:99-112`
- `FoodRotate/Views/FoodEditorView.swift:189-196`
- `FoodRotate/Views/FoodEditorView.swift:241-245`

**證據**：轉盤改名寫入 `renamedNames`，抽樣 `pool` 會套用新名；但「我的清單」直接列 `customItems`，不會套 rename override，因此同一道自訂料理在設定頁可能仍顯示舊名。

編輯自訂料理時，程式註解說要「先清掉改名覆寫」，實作卻在 `update(item)` 前呼叫 `rename`。此時 store 裡仍是舊名稱，新名稱不相等，反而會留下新的 override；`customizationCount` 也可能把一項自訂料理重複計數。

**改善**：

- 自訂料理只保留一個名稱真相來源：直接更新 `customItems`；`renamedNames` 僅服務不可修改的內建料理。
- 或提供 `resolvedCustomItems` 給設定頁與編輯器共用，不能一頁讀 raw、一頁讀 resolved。
- 加測試：轉盤改名後設定頁一致；編輯後沒有殘留 override；改回原名計數正確；重啟後一致。

### P2-4：名稱被當成 winner 身分，允許同名後會高亮錯誤或還原錯誤

**位置**：

- `FoodRotate/Views/FoodCardList.swift:13`
- `FoodRotate/Views/FoodCardList.swift:61`
- `FoodRotate/Models/SpinRecord.swift:14`
- `FoodRotate/Models/SpinRecord.swift:42-44`

**證據**：自訂料理與改名都沒有阻止同名。候選清單以 `item.name == winnerName` 判斷勝出，歷史也用 `first { name == winnerName }` 找 winner。兩道同名時可能同時高亮，歷史則永遠取第一道的圖示／資訊。

**改善**：SpinRecord 新增穩定的 `winnerID`，UI 傳 winner ID 而不是 winner name；名稱只作顯示。舊紀錄可在 migration／fallback 中繼續用名稱解析。

### P2-5：歷史儲存與資料庫降級完全靜默，可能讓使用者誤以為紀錄已保存

**位置**：

- `FoodRotate/FoodRotateApp.swift:9-16`
- `FoodRotate/Views/RotateView.swift:303-304`
- `FoodRotate/Views/HistoryView.swift:103-114`

**證據**：SwiftData container 建立失敗時自動退回記憶體 container；儲存與刪除使用 `try?`。App 仍正常顯示，但紀錄可能在重啟後消失，使用者沒有任何提示，工程也沒有可追查的 error log。

**改善**：

- 保持 App 可啟動的降級策略，但把「歷史暫時無法保存」作為非阻擋提示顯示。
- 統一由 HistoryRepository／store 封裝 save error，保留可測的錯誤狀態。
- 至少用 `Logger` 記錄 container 與 save 失敗；測試記憶體模式、save failure 與重啟持久化。

### P2-6：README 導航與進度已過期，會誤導接手者

**位置**：

- `README.md:87-90` 仍寫 S5B 進行中。
- `Coder/README.md:16-18` 仍把 S2 當最新驗收，把 S3／S4 當下一步。
- `Coder/README.md:30-34` 仍把已經有「PM驗收-問題單去哪吃-通過.md」的問題列為進行中。

**影響**：新工程師照 README 走會優先處理已完成工作，且可能漏讀真正最新的 S5B 驗收與後續 QC 問題。

**改善**：指定一份單一的 `PROJECT_STATUS.md` 或在根 README 維護「Current / Done / Known Issues」；歷史文件保留原狀，但入口文件只指向最新結論。每次 PM 驗收通過時，把更新入口文件列為 Definition of Done。

### P3-1：目前的自動測試沒有覆蓋風險最高的狀態層

**證據**：43 個測試集中在 `FoodPicker`、資料 audit、token、輪盤文字、搜尋詞與圖示。沒有 `RotateViewModel`、`NearbySearchModel`、`WheelSpinner` completion lifecycle、`CustomFoodStore` 持久化、`SpinRecord` migration／restaurant restore 的測試，也沒有 UI test 或 snapshot test target。

**具體改善順序**：

1. 先把 MapKit、時間、隨機數、持久化與 Live Activity 抽成可注入的小介面。
2. 補狀態機單元測試：取消、過期 callback、快速切模式、重複搜尋、reset/spin race。
3. 補整合測試：餐廳歷史保存／還原、UserDefaults 重啟一致、SwiftData save failure。
4. 補少量 UI tests：首次啟動、兩模式切換、轉動中換條件、歷史還原、深淺模式關鍵頁。
5. UI 測試不必覆蓋每個像素；視覺 token 可保留現有數值測試，再對 5–8 個關鍵畫面做 snapshot baseline。

### P3-2：視覺驗收證據放在暫存路徑，無法由下一位工程師重現

**位置**：`Coder/工程回覆-S5B其餘四頁-給PM.md:167` 附近。

**證據**：工程回覆引用 `/private/tmp/...` 截圖；暫存檔不屬於 repo，也沒有 UI／snapshot coverage。Bugbot 對本分支唯一提出的 finding 也是這一點。

**改善**：若團隊不想把大量 PNG 放進 repo，可保存精簡的驗收附件、產圖腳本與操作步驟；或在 CI 產出可下載 artifact。至少要讓深淺模式、Dynamic Type、reduce motion、全域 segmented appearance 的驗收可重跑。

---

## 四、PM 建議排程

### 第一批：立即穩定核心流程（建議同一個修正週期）

1. P0-1 轉盤完成 Task 身分與取消。
2. P1-1／P1-2 搜尋 request ID、取消與 stale result 防護。
3. P1-3 MapKit not-found 正規化與 per-term 容錯。
4. 為以上三類新增可重現的單元／狀態機測試。

**驗收門檻**：快速操作 50 次無崩潰；切模式後舊結果不回灌；改條件只接受最後一次請求；單一搜尋詞找不到不會抹掉其他有效結果。

### 第二批：補齊資料與發布正確性

1. PM 決定餐廳歷史策略，處理 P1-4。
2. 補 `PrivacyInfo.xcprivacy`、產 privacy report、做 archive validation。
3. 修正距離文案與 map camera。
4. 處理自訂料理 rename 單一真相來源與 winner ID。

### 第三批：建立回歸防線與文件治理

1. 補 RotateViewModel／NearbySearchModel／CustomFoodStore／SpinRecord 測試。
2. 保存可重現的 UI 驗收證據。
3. 更新根 README 與 Coder README，只把最新狀態放在入口。
4. 建立 release checklist：Debug tests、Release build、archive、privacy report、深淺模式、Dynamic Type、reduce motion、定位拒絕／逾時、離線、餐廳搜尋 fallback。

---

## 五、驗證紀錄

### 已執行

- `xcodebuild test -project FoodRotate.xcodeproj -scheme FoodRotate -destination 'platform=iOS Simulator,name=iPhone 17 Pro' ...`
  - 結果：`** TEST SUCCEEDED **`
  - Swift Testing：`43 tests in 7 suites passed`
- `git diff --check`
  - 結果：通過。
- `git status --short`
  - 建立本報告前：工作樹乾淨。
- JSON 資料抽查：
  - 50 筆資料。
  - ID unique：50／50。
  - 沒有重複名稱。
  - 沒有空名稱、空 emoji、空 tags 或優缺點數量／長度違規。
- Bugbot：
  - 對 `main...HEAD` 執行一次。
  - 沒有找到分支差異中的高／中嚴重度功能 bug。
  - 1 項 P3：視覺驗收證據不可重現。

### 本次未能直接證明

- 沒有在真機測定位品質、電話、Apple／Google 地圖跳轉與 Live Activity。
- 沒有 App Store Connect 帳號與正式 archive，因此 privacy manifest 問題屬於「依官方規則確認的發布缺口」，尚未做上傳驗證。
- P0／P1 的非同步問題是依目前程式控制流可確定的缺陷，但本次遵守「不得改程式碼」，沒有加測試探針或故障注入程式來觸發畫面錄影。

---

## 六、完成定義建議

下一版要宣告 QC 通過，至少應滿足：

- P0 全清。
- P1 全清，或 PM 有書面接受風險與替代產品行為。
- 43 個既有測試持續通過，並新增非同步／歷史／持久化測試。
- Debug、Release、archive 均成功。
- Privacy report 與 App Store validation 通過。
- 真機或模擬器實測：快速換條件、切模式、定位拒絕、定位逾時、無搜尋結果、fallback、餐廳歷史、深淺模式、reduce motion。
- 根 README 與工程入口文件反映同一份最新狀態。

在完成以上項目前，較準確的專案狀態是：**核心離線功能品質良好、視覺改版已完成，但非同步邊界、餐廳歷史與發布合規仍需一輪硬化。**

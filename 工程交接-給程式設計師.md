# 食物轉盤 Food Rotate — 工程交接（給負責整合的程式設計師）

**日期**：2026-08-11 ／ **整理**：PM
**基準點**：`6b2a37f`「拿掉語言模型後的完整可用版本」— 所有改動以此為 diff 基準
**視覺方向**：已定案 **B（灶）+ 折衷案**，見 `Design/PM決策-視覺方向v1-回覆.md`

---

## 一、你負責什麼

| 你的 | 不是你的 |
|---|---|
| `FoodRotate/Theme.swift`（S1 新增，收攏所有 token） | 色票怎麼訂 → 設計師 |
| `WheelView` 的 Canvas 重寫（兩行排版、動態字級、圖示） | 圖示長什麼樣 → 設計師 |
| 圖示資產進 asset catalog + 「分類×形態 → 圖示」對照表與 fallback | 對照表要對到哪個圖 → 設計師 |
| 中選轉場、結果頁深色主題 | 轉場的節奏與幅度 → 設計師規格 |
| `Tools/make-icon.swift` 改讀同一份 token | — |
| 補一道 DEBUG 期的資料／圖示覆蓋率檢查 | — |
| **「改完之後行為完全沒變」** | 產品規則要不要改 → PM |

設計師交 `設計規格-Theme-v1.md`＋`.html`，你照它實作。規格不清楚就問，不要自己猜一個值填進去。

**S1 分成兩半，規格在前、實作在後**（2026-08-11 修正，原本兩份文件的說法互相矛盾）：

| 子階段 | 負責 | 產出 |
|---|---|---|
| **S1a** | 設計師 | `設計規格-Theme-v1.md`＋`.html`（完整 token，淺深兩套） |
| **S1b** | 程式設計師 | `Theme.swift`、轉盤八色換值、`make-icon.swift` 改讀同一份 token |

S1a 未交付前，S1b 只能做「已定案色票」的骨架，**沒定案的數值一律留空，不得自行填入**。

---

## 二、環境與現況

| 項目 | 值 |
|---|---|
| 專案 | Xcode，2 個 target：`FoodRotate`（App）、`FoodRotateWidgetExtension`（Live Activity） |
| iOS Deployment Target | 26.0 |
| Swift | 6.0（嚴格併發，UI 層一律 `@MainActor`） |
| 架構 | SwiftUI + `@Observable`，資料層 SwiftData（只有 `SpinRecord` 一個 model） |
| 依賴 | **零外部套件**。MapKit / CoreLocation / ActivityKit 都是系統框架 |
| 測試 | **一個都沒有，連 test target 都沒有** |
| 規模 | 約 3700 行 Swift，25 個檔 |

---

## 三、程式地圖

### `Core/` — 純資料層，**移植 Android 時要逐行對翻的部分**

只用 `Foundation`，不碰 SwiftUI／SwiftData／`@Observable`。**請維持這條界線**，不要為了方便把 `Color` 或 `View` 塞進來。

| 檔 | 內容 |
|---|---|
| `FoodItem.swift` | 料理型別。`id` 前綴決定來源：`custom-` 自訂、`place-` 店家。`displayEmoji` 有防呆（非 emoji 一律回 🍽️） |
| `FoodTag.swift` | 33 個標籤、6 個維度。`rawValue` **同時是**畫面文字與 `foods.json` 裡的字，三者同一份 |
| `FoodPicker.swift` | 抽樣純函數。**這支是產品規則的所在地，見第四節** |
| `FoodLibrary.swift` | 從 bundle 載 `foods.json`。標籤打錯會 `fatalError` |
| `foods.json` | 50 道資料 |

### `Views/` — 這輪要動的地方

| 檔 | 行數 | 這輪的角色 |
|---|---|---|
| `WheelView.swift` | 281 | **S2 主戰場**。`palette` 硬寫在 110 行；扇形與文字全部 `Canvas` 手繪；`WheelSpinner` 是自訂緩動狀態機 |
| `RotateView.swift` | 496 | 主畫面 + `RotateViewModel`。S3 要壓縮第一屏 |
| `FilterBar.swift` | 188 | S4 改網格。**離產品規則最近的 View，小心** |
| `FoodCardList.swift` | 245 | S4 改高密度列 |
| `ResultSheet.swift` | 132 | S3 深色化 + 接中選轉場 |
| `FoodEditorView.swift` | 269 | S4 沿用同一套標籤元件。**圖示 fallback 的缺口在這裡** |
| 其餘 | — | S5 套 token |

### 其他

- `Services/NearbySearch.swift`（465 行）：定位 + MapKit 搜尋，這輪**不動**
- `Shared/GenerationActivityAttributes.swift`：App 與 Widget 共用，改它要兩個 target 一起編
- `Tools/make-icon.swift`：獨立 script，不編進 App，用 `swift Tools/make-icon.swift <輸出路徑>` 跑

---

## 四、八條不可改的產品規則

完整版在 `專案現況彙整-給設計師.md` 第六節。**視覺可以整個重做，行為不能改。** 對你最關鍵的三條：

1. **忌口（無牛／無豬／無海鮮／素可）永遠不會被自動放寬。** 實作在 `FoodPicker.pick()` 第一步：先用 `isSuperset(of: restrictions)` 篩，篩完是空的就直接回報 `isOverConstrained`，不進放寬迴圈。**這段邏輯一個字都不要動。**

2. **放寬是「往下補」不是「重來」。** `absorb()` 逐層累積、`seen` 去重、`pool` 的順序就是符合程度的順序。第 82 行有註解寫明「這裡不能再洗一次牌」—— 這是實跑抓到的 bug 修正，重構時很容易手滑洗掉。

3. **「去哪吃」模式只顯示菜系那一列標籤。** S4 改網格時，`activeDimensions` 這個參數要繼續有作用，不能因為改版面就把六個維度全列出來。

**驗收時我會逐條比對。** 如果實作規格必須碰到這些行為，先跟我說，不要自己權衡。

---

## 五、已知的坑

| 坑 | 說明 |
|---|---|
| **沒有測試** | 改 `FoodPicker` 或 `FilterBar` 沒有任何東西接得住你。建議 S1 順手補上 `FoodPicker` 的單元測試（純函數，好測），特別是「忌口不放寬」與「逐層往下補」兩個案例 |
| **`foods.json` 沒有自動化檢查** | 標籤打錯只會在執行期 `fatalError`。圖示改成靠標籤決定之後，缺標籤會變成「畫面上沒有圖」。**這是你要補的檢查的主要理由** |
| **icon 色盤是刻意加深的** | `Tools/make-icon.swift:23` 的註解寫明：icon 在桌面只有 60pt，粉彩會糊成灰。收攏 token 時**要保留這道加深轉換**，不能把兩邊壓成同一組數值。檔頭第 5–6 行和第 21 行的註解已過時（說「重跑就能同步」「與 palette 相同」），順手修掉 |
| **`SpinRecord` 有相容包袱** | `engineRawValue` 欄位保留但不再寫入（拿掉要 migration）。舊紀錄的候選清單解不開是**刻意接受**的，不要「順手修好」 |
| **店家借用 `FoodItem`** | `place-` 前綴、`category` 放距離文字、`pros` 放地址電話網站。這是有記錄的技術債（`大改紀錄` 3.11），這輪**不要重構它**，S2 只需要讓圖示系統對 `isPlace` 有 fallback |
| **Canvas 不是 SwiftUI 排版** | 轉盤的字要自己算斷行、字級、旋轉與左半邊的鏡射（`drawLabel` 有註解說明為什麼要多轉 180°）。兩行排版是這輪最硬的一段 |
| **`WheelSpinner` 不用 `withAnimation`** | 角度由時間算出來的純函數，跨格觸覺在起轉前一次排程完。中選轉場要接的是 `finish()` 那個時間點，不要改成系統動畫 |

---

## 六、S1 / S2 範圍與驗收

分批驗收，**S1+S2 做完先停下來給 PM 看**，這兩階段解掉兩個 P0。

### S1 設計系統打底
- 新增 `Theme.swift`：色票／字級／間距／圓角／線與陰影，淺深兩套
- 轉盤八色換成 B 方案定案值（淺底 8 色、深底 8 色，見 PM 決策文件第一節）
- `make-icon.swift` 改讀同一份 token（保留加深轉換）
- 建議順手：`FoodPicker` 單元測試、`foods.json` 的 DEBUG 期檢查

**此時畫面看起來幾乎沒變，這是正常的。**

### S2 轉盤本體
- 兩行排版 + 動態字級 → **零截斷**，4／6／8／10／12 格全部要成立（最長菜名 7 字：西班牙海鮮燉飯、韓式泡菜豆腐鍋）
- 圖示系統取代 emoji（含自訂料理與店家的 fallback）
- 中選瞬間：該格提亮放大 0.35s、其餘降透明度、success 觸覺

### PM 驗收標準
1. 八條產品規則逐條比對，行為零變化
2. 12 格滿載 + 最長菜名，零截斷
3. 淺深兩色模式都看過
4. `git diff 6b2a37f` 的範圍與說明一致，沒有夾帶

---

## 七、工作約定

| 項目 | 約定 |
|---|---|
| **Git** | **commit 或 push 之前一律先向 PM 報告內容，取得同意才執行。** 這條對所有人生效，沒有例外 |
| 基準 | 一律以 `6b2a37f` 為 diff 基準，方向不對可整包退回 |
| 範圍 | 只做當前階段。看到順手可以修的東西，**寫進報告，不要順手改** —— 夾帶會讓驗收失去意義 |
| 產品規則 | 第四節八條全部遵守。需要碰到就先問 PM |
| 註解 | 這個專案的註解寫的是「為什麼」不是「做什麼」，密度偏高。**請照同樣的風格寫**，尤其是你做了取捨的地方 |

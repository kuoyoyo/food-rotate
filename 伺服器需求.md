# 食物轉盤 — 自架推論端需求與實測

> **本文描述的功能已不存在。** 自架語言模型那條路在 2026-08-11 整個拿掉了，
> 換成內建資料庫 + 標籤篩選 —— 原因與取捨寫在 [`大改紀錄-拿掉語言模型.md`](大改紀錄-拿掉語言模型.md)。
> 這份實測留著是因為它是那個決定的依據，不是使用說明。**App 不需要任何伺服器。**

**環境**：Ubuntu / RTX 3080 10 GB / 32 GB RAM / Ollama 0.32.5 / `<內網主機>:11434`
**方法**：用 App 正式路徑上的 prompt 與解析器發請求，非簡化測試
**變更**：無。只發 HTTP，未登入主機。

---

## Client 端的請求形狀

`POST /v1/chat/completions`，`stream: false`，`temperature: 0.9`，兩則 message（system + user）。
不使用 `response_format`，schema 以文字寫在 system prompt。輸入約 1.2k 字，期望輸出約 2k 字（12 個物件的 JSON）。
Client timeout：request 180s / resource 240s。

解析端容錯：markdown fence、前後贅字、`<think>` 區塊、缺 `items` 外層的裸陣列。

---

## 實測數據

### `qwen2.5:14b`（Q4_K_M）

三種請求形狀交叉比對，排除端點與取樣參數的影響：

| 條件 | 耗時 | 輸出 | finish_reason | 結果 |
|---|---|---|---|---|
| `/v1` + temp 0.9（正式路徑） | 29s | 1706 字 | stop | 完整 12 |
| `/v1` 不帶 temp | 20s | 1935 字 | stop | 完整 12 |
| `/api/chat` + temp 0.9 | 25s | 1766 字 | stop | 完整 12 |
| `/api/chat` + `num_ctx=8192` | 46s | 2026 字 | stop | 完整 12 |

**預設 4096 對這個模型沒有問題**，不需要調 context。

內容品質（同一組 prompt，App 端去重後）：

- 「想吃日式料理」→ 親子丼、壽司、拉麵、天丼、日式炸豬排、味噌湯、烏龍麵、章魚燒
- 「清爽一點」→ 涼拌小黃瓜、海帶芽湯、涼麵、涼拌豆腐、昆布柴魚湯、海草沙拉、涼拌黑木耳

已知偏差：偶發簡體（涼拌小**黄**瓜）；分類逸出 enum（水煮魚 → 「川式」，不在 App 的 16 類內）；
emoji 欄位曾填入漢字「亲」。三項 client 端都已處理。

### `qwen3.5:0.8b`（873M，Q8_0，thinking model）

| 條件 | 耗時 | thinking | content | 結果 |
|---|---|---|---|---|
| `/v1`，預設 `num_ctx=4096` | 13s | — | **0 字** | `finish_reason=length` |
| `/api/chat`，`num_ctx=16384` | 15s | 4278 字 | 2199 字 | 完整 13 |
| `/api/chat`，`num_ctx=16384`，`think:false` | 3s | 0 | 1420 字 | **JSON 無效** |

三點值得注意：

1. **`/v1` 層把 reasoning 吃掉但仍計入預算。** 4096 之下 reasoning 就把額度用光，
   回應是 `finish_reason=length` 配 `content=""`，client 端看到的是「空回應」而不是「被截斷」，
   容易誤判成連線或格式問題。這個模型至少要 16384。
2. **關掉 thinking 之後 JSON discipline 直接崩掉。** `think:false` 那次輸出
   `"name": 「清吉生鮨」` — 用中文引號當字串定界符。速度從 15s 掉到 3s，但完全不可用。
3. 品質不足以支撐這個任務：「想吃日式料理」只回 3 道（要 12），且為簡體
   （清蒸三文鱼沙拉、海胆烤寿司卷、蒸鱼生蚝套餐）；「清爽一點」7 道中
   「日式清蒸小魚頭（不含油）」重複三次，並產生「法式烤麵包夾小魚頭」；
   cons 欄位填的是 pros 內容（「魚刺較少」「無油脂，低熱量」）。

**結論：0.8B 不適用。** 建議維持 `qwen2.5:14b`。

---

## 需要處理的：模型未常駐

`/api/ps` 回空清單。觀察到的序列：

1. 第一次請求 61s 未完成，被 client 的當時 60s timeout 中斷
2. 緊接的第二次請求 49s 後回傳**結構不完整的 JSON**
3. 之後每次都是 20-29s、輸出完整

推測第 2 次打在權重載入尚未完成的視窗內。無論成因，模型常駐可根治：

```
[Service]
Environment="OLLAMA_KEEP_ALIVE=-1"
```

代價約 9 GB VRAM 常駐。若這台有其他用途，`30m` 之類的有限值也可以，
只要涵蓋使用者連續操作的間隔即可。

client 端已把 timeout 從 60s 提高到 180s 以吸收冷啟動，但那是遮蔽不是修復。

---

## 其他

**`OLLAMA_HOST=0.0.0.0:11434` 目前已生效**（跨機可達），重裝或改設定時請保留。
client 走 `http://` 區網位址，iOS 端已在 `Info.plist` 開 `NSAllowsLocalNetworking`。

**若要調 context 到 16384**，14B Q4_K_M（~9 GB）在 10 GB 卡上會很緊，
建議換 Q4_K_S 或 IQ4_XS（~8.0-8.4 GB）留出 KV cache 空間。

**繁中偏差若要根治**，可評估 Llama-3-Taiwan-8B-Instruct / TAIDE LX-8B / MediaTek Breeze-2。
代價通常是 structured output 穩定度下降；client 端解析有容錯，但仍需實測。
換模型只需改 App 設定頁的模型名稱欄位，不動程式。

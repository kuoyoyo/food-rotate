# 工程回覆 — S1b 收尾（給 PM）

**日期**：2026-08-11 ／ **提出**：程式設計師
**依據**：`Design/PM核可-設計規格Theme-v1.md` 第五節、`Design/設計規格-Theme-v1.md`
**狀態**：標籤改名**已 commit**（`26d1d3b`）；token 填完的部分**未 commit，等 PM 同意**

---

## 一、六項指示的執行結果

| # | 指示 | 結果 |
|---|---|---|
| 1 | 標籤定名「肉食」，連同 `foods.json` 一起改，改完再 commit | ✅ **已 commit `26d1d3b`**，只有一筆，沒有留「主餐」再改名 |
| 2 | 依規格填完 `DesignTokens.swift` 的留空項目（含 `Dark.sauce`） | ✅ |
| 3 | 轉盤盤色改為「fill + 文字色」成對的結構 | ✅ |
| 4 | 實作取色順序表與淺階變體 | ✅ |
| 5 | 實作 icon 加深公式，`make-icon.swift` 解除卡住 | ✅ 色彩部分完成，**構圖那項要請示，見第四節** |
| 6 | 圓角要不要改語意命名，由工程決定 | ✅ 決定**維持大小命名**，理由見第三節 |

**留空清單現在是空的。** 字級、間距、髮絲線顏色、深色四個表面 token、icon 加深公式，全部填完。

---

## 二、驗算：我把規格的數字全部自己重算過一次

規格說推導表是「供工程驗證」，所以我真的驗了。

### 對比值 —— 16 個全部吻合，且全部 ≥ 4.5 ✅

淺底八色、深底八色，用獨立實作的 WCAG 相對亮度公式重算，**與規格印的值一位不差**。
`Dark.sauce` 在深卡片上 4.64、`Dark.text` 13.43，也都吻合。

### 兩條公式 —— 六項吻合，三處有出入

| 項目 | 規格印的 | 公式算的 | 判定 |
|---|---|---|---|
| icon 辣椒／蛋黃／抹茶／藍染／芋泥／焙茶 | — | — | ✅ 完全一致 |
| icon 蔥綠 | `#456A56` | `#456D57` | ⚠️ 差 3/255 |
| icon 青瓷 | `#3B6A76` | `#396B78` | ⚠️ 差 2/255 |
| **蔥綠淺** | `#7BA88E` | `#6F9882` | ⚠️ **差 12/255，不是雜訊** |

**前兩處**是四捨五入雜訊，肉眼不可能分辨，不影響任何判斷。

**第三處要請設計師確認。** `#7BA88E` 不是從現行母盤推出來的 —— 它是拿**微調前**的舊蔥綠
`#5E8C72` 套 ×0.82／×1.20 的結果（我驗算過，分毫不差）。規格第六節的 icon 表有註明
「蔥綠、青瓷兩列是套用第一節微調後的值重算」，但二-3 的淺階表**沒有跟著重算**。

### 我的處理與理由

**採用公式，不採用表上的值。** 三個理由：

1. 規格第六節寫明「純函數，不需要另存一份色票」，公式是定義，表是驗證用的
2. 把 `#7BA88E` 抄成常數 = 又多一份跟母盤脫鉤的色票，那正是這輪要解掉的問題
3. `#6F9882` 的墨字對比是 **5.09**，仍然過 AA（表值 `#7BA88E` 是 6.15）

差別是「蔥綠淺這一格比規格稿再深一點點」。**如果設計師是刻意要那個更淺的值，
說一聲我改成明確常數**，但那樣就要在規格裡把它標記為「不由公式推導」。

---

## 三、圓角：維持大小命名

規格第五節給了語意歸屬（10 用在卡片／提示框／載入框／sheet 分區塊，8 用在卡片內圖示底／
小型狀態標記；按鈕與 chip 維持 `Capsule`），設計說命名由工程決定。

**維持 `radiusLarge` / `radiusSmall`。** 理由：同一個值服務好幾種語意，叫成 `radiusCard`
之後，「提示框為什麼用卡片的圓角」會變成每次都要解釋一次的事。語意歸屬我寫進註解裡了，
查得到但不綁死命名。

---

## 四、要請示的一件事：icon 的「八格改六格」現在做還是 S5？

規格與核可文件對這件事的說法不一致：

| 出處 | 說法 |
|---|---|
| `PM核可` 第一節第 6 項 | 「icon 由八格改六格」✅ **核可** |
| `PM核可` 第五節第 5 點（給工程的指示） | 只說「實作 icon 加深公式，解除卡住」 |
| 規格第六節 | 「icon 的**完整重繪是 S5 的事**。這裡只定義色彩推導規則」 |

**我照最保守的讀法做：只做色彩，構圖維持八格。** 依工作約定「只做當前階段，看到順手可以
修的東西寫進報告不要順手改」。

改成六格 + 中心刀叉放大到 40% 是十幾行的事（六格取色序列已經實作好了，直接呼叫
`slotOrder(for: 6)` 就有）。**要現在做請說一聲**，不然我留到 S5。

---

## 五、驗證

| 項目 | 結果 |
|---|---|
| App 建置 | ✅ |
| 單元測試 | ✅ **18 個全過**（S1b 原有 11 + 這次新增 7） |
| `make-icon.swift` | ✅ 編得過、跑得出圖，不再卡住 |
| 淺色模式實機 | ✅ 截圖確認 |
| 深色模式實機 | ✅ 截圖確認 |

### 實機看到的（6 格）

- **淺色**：抹茶那格（披薩）是**墨字**，其餘五格白字 —— 對比修正確實生效
- **深色**：盤色換成深底那套，**六格全部墨字**，與規格一致
- 取色是 `0,2,3,4,6,7`（辣椒／抹茶／蔥綠／青瓷／芋泥／焙茶），跳號取，相鄰色相拉得開

### 新增的 7 個測試（`DesignTokenTests.swift`）

最重要的一支是**對比門檻**：4／6／8／10／12 五種格數 × 深淺兩底的**每一格**，
逐格驗 fill 與 ink 的對比 ≥ 4.5。

寫這支的理由就是規格第一節那件事本身 —— 定案八色時沒人量對比，一路到 S1a 才被發現。
**對比不合格是看不出來的那種壞**：畫面照常顯示，只是有些人讀不到。這種東西不能靠
「下次有人想到要量」。

其餘六支：表面與主色的對比、取色順序表逐格比對規格、同一盤不撞色、
表外格數（抽不滿時只有 1–3 道）不會當掉、icon 公式與規格表的容差比對、HSB 換算可逆。

---

## 六、看到但沒有順手改的

| 東西 | 為什麼沒動 |
|---|---|
| 轉盤的白色分隔線與外圈（`.white.opacity(0.75)` / `0.9`） | 規格沒有定義，深色模式下外圈偏亮。屬 S2 轉盤本體 |
| 按鈕與 tab 仍用系統 accent，不是「醬」 | token 已經備好（`Theme.sauce(for:)`），套到各 View 是 S3–S5 |
| 頁面底仍是系統色，不是 `#EFEAE0` | 同上。S1 的範圍是「只換 token 不換元件」 |
| 現有程式碼裡的 6／10／14 三個奇數間距 | 靠攏要各 View 各自改，階梯已經定義好了，屬 S3–S5 |
| Widget 拿不到 token | 要搬去 `Shared/`，會動 pbxproj。Widget 目前沒用到色票 |

---

## 七、Commit 計畫（**等 PM 同意才執行**）

```
FoodRotate/DesignTokens.swift          （改：填完留空項目 + 兩條推導公式 + HSB）
FoodRotate/Theme.swift                 （改：字級／間距／深色／盤色 API）
FoodRotate/Views/WheelView.swift       （改：接取色順序表與每格文字色）
Tools/make-icon.swift                  （改：解除卡住，改讀加深公式）
FoodRotateTests/DesignTokenTests.swift （新增）
```

訊息草稿：

```
S1b：依核可的設計規格填完 token，解除 icon 腳本的卡住

留空清單清空：字級（對應系統 TextStyle，保住 Dynamic Type）、間距八階、
髮絲線實色、深色四個表面，以及規格補上的 Dark.sauce——淺色的醬疊在深卡片上
對比只有 2.36。

轉盤改成「底色 + 文字色」成對：定案八色裡蛋黃與抹茶配白字只有 2.5，
不能再無條件白字。10／12 格改用同色相的淺一階變體，不再 index % 8 撞色。

icon 加深與淺階變體都做成從母盤推導的純函數，不另存色票。
規格印的推導表有三處與公式不符，其中蔥綠淺是拿微調前的舊值推的，已回報。

測試釘住對比門檻：五種格數 × 深淺兩底逐格驗 ≥ 4.5。對比不合格是看不出來的
那種壞，不能靠下次有人想到要量。
```

`Design/` 的三份新文件與根目錄兩份回報也還未追蹤，要不要一起進請指示。

---

## 八、還在等的

- **蔥綠淺**：`#6F9882`（公式）還是 `#7BA88E`（規格表值），請設計師確認 —— 第二節
- **icon 六格構圖**：現在做還是 S5 —— 第四節
- **圖示單色**：規格八-3a 已經正式確認是刻意的、而且是硬性的，我看到了，這題**不再擋 S2**

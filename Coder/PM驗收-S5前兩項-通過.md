# PM 驗收 — S5 前兩項 **通過**

**日期**：2026-08-12 ／ **驗收**：PM
**對象**：`工程回覆-S5前兩項-給PM.md`
**結論**：**兩項都通過。另有一件新進來的事要你做（設計剛改了兩個圖示）。**

---

## 一、「以後都不要」的二次確認：實機走過完整流程

| 步驟 | 結果 |
|---|---|
| `⋯` → 以後都不要 | ✅ 跳出確認框 |
| 標題 | ✅ **以後都不要「牛丼」？**（帶菜名） |
| 說明 | ✅ 「這道菜不會再出現在轉盤上。**可以在設定的「我的清單」裡改回來。**」 |
| 點框外取消 | ✅ 「牛丼」還在清單裡 |

**救回來的路徑照 `SettingsView.swift:41` 的實際標籤寫**，不是照我派工單的描述 ——
指出去的路徑要真的存在，這個查證做得對。指錯路比不指路更糟。

### 記一筆：「取消」按鈕在畫面上看不到

你程式碼裡有 `Button("取消", role: .cancel)`，但實機沒有可見的取消鈕 ——
從 `Menu` 觸發的 `confirmationDialog` 在 iOS 26 走 popover 呈現，
系統會省略 cancel，靠點框外關閉（這是 HIG 的做法，不是你漏了）。

**我判斷可接受**：防手滑的目的達到了。Menu 項目與確認鈕在畫面上有明顯垂直距離，
同一個位置連點兩下不會誤觸。

---

## 二、`reduceMotion`：通過，包含你自己抓到的那處不符

### 你抓到的規格不符

| 對象 | 規格 | 原本 |
|---|---|---|
| 背景壓暗層 | `reduceMotion` 時 0.15 | 一律 0.22 ❌ |

我看了 `RootView.swift` 的 diff，改法對，而且註解寫了「跟 sheet 一起」——
**這處是你自己逐條對規格讀出來的，不是我退回的。** 主動對規格複查抓到自己的漏，
比等驗收退回好。

### 五項的處置

| # | 對象 | 判定 |
|---|---|---|
| 1 | 中選格不放大 | ✅ 你實測 |
| 2 | 圖示轉場取消 | ✅ 你實測 |
| 3 | 最終狀態正確 | ✅ 你實測 |
| 4 | Sheet 淡入而非推入 | **不擋，接受你的推論** |
| 5 | 壓暗層 0.15 淡入 | **不擋，同上** |

第 4、5 項我**不再另外花時間抓過程畫面**，理由是你的推論鏈成立：
這兩個轉場跟第 1 項是同一個 `reduceMotion` 運算式選出來的，而第 1 項已經直接觀察到
分支被走了 —— **未觀察到的只有插值的樣子，不是分支有沒有被選中。**

你把它老實列在「已實作，未實測」而不是打勾，這個分類正是 S4 收緊過的標準，維持住了。

> 順帶記你那個做法：`xcrun simctl spawn booted defaults write com.apple.Accessibility
> ReduceMotionEnabled -bool true`。用指令開輔助使用設定，比點畫面快也不會忘了關。

---

## 三、⚠️ 新進來的一件：設計今天改了兩個圖示，還沒進 catalog

kuoyo 直接向設計指正了兩件（**這不在你的派工單裡，是新的**）：

| 圖示 | 問題 | 新版 |
|---|---|---|
| `form-meat` 肉食 | **讀起來像魚**（平滑橢圓 + 右側突出物 = 魚身 + 魚尾） | **厚切牛排俯視** |
| `form-snack` 小吃 | 丸子串不是台灣的東西 | **包子** |

我逐檔比對過九個圖示：

```
form-bread 同步 / form-hotpot 同步 / form-light 同步 / form-noodles 同步
form-rice 同步 / form-soup 同步 / form-unknown 同步
★ form-meat 不同步
★ form-snack 不同步
```

**這兩個要重新匯入。** 你上次把比對換成一道指令了，直接跑那個就會抓到。

匯完之後我會實機看這兩個圖示在 24pt（候選清單）與 17pt（12 格轉盤）的辨識 ——
特別是「小吃（包子）vs 飯食（碗）」，設計說區別在「飯食的碗身在基準線下方，
包子下方什麼都沒有」，那是要實機確認的。

---

## 四、你提早回報的技術限制，我已經轉給設計

> 自訂料理可能沒有吃法標籤，落到中性餐具圖示。**使用者自己挑的 emoji 被換成
> 一個沒有辨識度的餐具符號。**

這個資訊有價值，它把「換不換」從美感問題變成**代價問題**。設計在做那個判斷時會看到。

---

## 五、其他

| 項目 | 結果 |
|---|---|
| 建置與測試 | ✅ 51 個全過 |
| 紅線：套 token 不等於改行為 | ✅ 這兩項都不是套 token；設定頁、歷史頁、我的清單一行未動 |
| `FoodPicker.swift` | ✅ 從 S1 到現在仍零變更 |

---

## 六、下一步

1. **匯入 `form-meat` 與 `form-snack`**（現在就能做）
2. App Icon：`Design/設計規格-AppIcon-v1.md` 我已核可
   （見 `Design/PM核可-設計規格AppIcon-v1.md`）。**注意它要你改 `make-icon.swift`
   而不是把 SVG 丟進 catalog** —— 理由是色盤不能再分岔一次
3. 轉盤分隔線與外圈（S2 掛件）：規格第七節有定案，跟 icon 是同一條規則
4. 其餘四頁套 token：等設計的 S5-B

這批 commit 等圖示匯入後一起。

# 工程回覆 — S1 開工前提問（給 PM）

**日期**：2026-08-11 ／ **提出**：程式設計師
**回應文件**：`工程交接-給程式設計師.md`、`Design/PM決策-視覺方向v1-回覆.md`
**基準點**：`6b2a37f`（工作區乾淨，未追蹤：`Design/`、`工程交接-給程式設計師.md`、本文件）

---

## 零、先確認我讀懂了範圍

| 階段 | 我要做的 | 我不碰的 |
|---|---|---|
| S1 | 新增 `Theme.swift` 收攏 token；轉盤八色換 B 案定案值（淺深兩套）；`Tools/make-icon.swift` 改讀同一份 token **並保留加深轉換** | 色票怎麼訂、字級怎麼定 |
| S2 | `WheelView` Canvas 兩行排版 + 動態字級；圖示系統取代 emoji（含自訂料理／店家 fallback）；中選轉場 0.35s + success 觸覺 | 圖示長相、對照表對到哪張圖、轉場節奏幅度 |
| 全程 | 行為零變化 | 八條產品規則。需要碰到會先問 PM |

已確認的現況：`WheelView.palette` 硬寫在第 110–119 行，是 RGB 浮點數不是 hex，換色時會整段換掉。`Core/` 只用 Foundation 這條界線我會維持，`Theme.swift` 不會被 `Core/` 引用。

**以下三題請 PM 裁示，未得到答覆前我不動任何一行程式碼。**

---

## 問題一（擋住開工）：`設計規格-Theme-v1.md` 還沒有，S1 要怎麼起手？

**現況**：`Design/` 目前只有 `設計提案-視覺方向-v1.md`／`.html` 和 `PM決策-視覺方向v1-回覆.md`，**沒有規格文件**。

**矛盾點**：

- `工程交接-給程式設計師.md` 第一節：「設計師交 `設計規格-Theme-v1.md`＋`.html`，你照它實作。」
- `PM決策-視覺方向v1-回覆.md` 第七節：「**S1 完成時**交 `設計規格-Theme-v1.md`。」

兩份文件把規格分別放在 S1 的**前**與**後**。我需要知道實際順序。

**目前已定案、我可以直接用的**（PM 決策第一節）：主色 `#9B3B2C`；頁面底 `#EFEAE0`／卡片 `#F8F5EE`／文字 `#241E18`；轉盤八色淺底與深底共 16 色；圓角 10／8；陰影幾乎取消改 1px 髮絲線。

**還沒有數值、我不會自己猜的**：字級階層、間距階層、髮絲線的顏色與透明度、深色模式的頁面底／卡片／文字、icon 加深規則的具體公式、深色結果頁的範圍界定。

| 選項 | 做法 | 代價 |
|---|---|---|
| A | 等設計師交完整規格再開工 | S1 停擺，但零返工 |
| **B（我建議）** | 先用已定案的色票把 `Theme.swift` 骨架與 `make-icon.swift` 串起來，**字級／間距／髮絲線／深色底色留空位不填**，規格到位再補 | 色票部分不會白做；骨架可能因規格結構微調 |
| C | 我照現況推一套完整 token 交設計師與 PM 過目 | 等於我代設計師決定，不符分工 |

**建議 B 的理由**：色票已經定案，這部分不會白做；沒定案的照工作約定我不猜。C 越界，A 太保守。

---

## 問題二：測試與資料檢查要不要納入 S1？

`工程交接-給程式設計師.md` 第六節寫「建議順手」，但這件事**不只是順手**：

- 補 `FoodPicker` 單元測試要**新增一個 test target**，會動到 `FoodRotate.xcodeproj`，`git diff` 會多出專案檔的變更
- 依工作約定「看到順手可以修的東西寫進報告，不要順手改」，我不確定這算不算夾帶

我的看法：**值得做，而且應該在 S1 就做。** 理由是 S2 之後 `FoodPicker` 完全沒有東西接得住，尤其「忌口不放寬」和「逐層往下補」這兩條是實跑抓到過 bug 的地方。但這是範圍決定，請 PM 拍板：

- [ ] 納入 S1，接受 `.xcodeproj` 出現在 diff 裡
- [ ] 不納入，S1 只做 token，測試另開一階段
- [ ] 只做 `foods.json` 的 DEBUG 期檢查（不需要 test target，變更較小），單元測試延後

---

## 問題三：S2 的圖示資產由誰產、什麼格式？

PM 決策第五節第 1 點已經把「自訂料理沒有分類沒有標籤要落在哪個圖示」這個缺口交給設計師回答，我等答案。但還有一層是工程面的、目前沒人講：

- **資產格式**：SF Symbols、SVG 轉 PDF（向量、可染色）、還是 PNG？我的建議是 **PDF 向量 + Template Rendering Mode**，這樣一份資產可以在淺底深底染成不同色，不必出兩套。
- **誰產出檔案**：設計師交檔案進 `Assets.xcassets`，還是交原始 SVG 由我轉？
- **命名規則**：我需要一份「分類×形態 → 圖示名」的對照表才寫得出查表邏輯，命名要能對得起來。

這題**不擋 S1**，但要在 S2 開工前有答案。

---

## 四、八條產品規則：逐條對照我的 S1／S2 工作

PM 已說明：**牽涉八條規則能不能動，原則上不能，但會看具體理由。** 我先自己逐條對過，
結論是 —— **上面三個問題沒有一個是在申請改規則**，三題都是工程面的順序、範圍與格式決定。

| # | 規則 | 我的工作會碰到嗎 | 說明 |
|---|---|---|---|
| 1 | 忌口永不自動放寬 | **不碰** | `FoodPicker.pick()` 第一步的 `isSuperset(of:)` 篩法與 `isOverConstrained` 回報，一個字不動。問題二要補的測試是**把這條行為釘住**，不是改它 |
| 2 | 收合狀態要看得到條件摘要 | S4 會碰到**外觀** | 摘要文字照舊產出，只換視覺。到 S4 我會再確認一次 |
| 3 | 放寬要老實說明放寬了哪個維度 | S3 會碰到**外觀** | `absorb()` 的逐層累積、`seen` 去重、`pool` 順序（第 82 行「不能再洗一次牌」）全部保留 |
| 4 | 「去哪吃」只顯示菜系那一列 | S4 會碰到 | `activeDimensions` 參數繼續有作用，改網格不等於六個維度全列 |
| 5 | 店家不顯示營業狀態 | **不碰** | S2 只讓圖示系統對 `isPlace` 有 fallback，不新增任何店家欄位 |
| 6 | 最少留 2 格才可刪卡片 | **不碰** | |
| 7 | 格數只能 4／6／8／10／12 | **是我的約束，不是衝突** | S2 的零截斷必須在這五個格數全部成立，12 格是最硬的一關 |
| 8 | 距離只能 1／3／5／10 公里 | **不碰** | |

### 唯一一處可能踩到「行為變更」的地方，先提前示警

不在八條裡，但性質相同，PM 決策第五節第 1 點已經埋了伏筆：

> 圖示 fallback 的兩個做法之一是「**在新增表單裡把形態設為必選**」。

這個做法會改到 `FoodEditorView.swift` 的 `canSave` —— 目前只檢查名稱非空，改成必選等於
**多一道使用者存不了檔的限制**，是行為變更，不是視覺變更。

**我的意見：優先選另一條路（給一個中性餐具圖示當 fallback）。** 理由是純視覺層、零行為風險，
而且「隨手加一道菜」本來就該低摩擦；為了讓圖示有得對而擋住存檔，是拿產品體驗換實作方便。

若設計師最後選的是「形態必選」，**我不會自己實作，會先回來向 PM 請示。**

---

## 我的等待狀態

問題一有答覆我就能開始 S1。問題二影響 S1 的範圍，也請一併給。問題三 S2 前給即可。

在此之前我不動程式碼。本文件與 `Design/` 一樣是未追蹤狀態，**沒有 commit，等 PM 指示**。

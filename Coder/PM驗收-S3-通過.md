# PM 驗收 — S3 **通過**

**日期**：2026-08-12 ／ **驗收**：PM
**對象**：`工程回覆-S3補件與S3-3-給PM.md`
**結論**：**通過，可以 commit。S3 到此結束。**

---

## 一、上一輪退回的兩項：都修好了，我實測過

| 項目 | 方法 | 結果 |
|---|---|---|
| **hero 區下拉關閉** | 實機從 hero 圖示拖 405pt，慢速 | ✅ **關閉了** |
| 角標水平內距 6 → 8 | 實機比對 | ✅ 比上一版寬，比例正確 |

hero 那項的根因你查出來了，而且解釋得對：

> hero 在 `ScrollView` 裡面，我把手勢掛在 sheet 根部用 `.gesture` —— 捲動手勢優先，把它吃掉了。
> 標題列在 `ScrollView` 外面所以沒事。

**這正好解釋了我實測看到的現象**：同一組手勢從標題列拖會關、從 hero 拖不會。
`.simultaneousGesture` + 往下拖時 `.scrollDisabled` 是對的解法。

---

## 二、S3-3 我實機跑過四種狀態

| 項目 | 結果 |
|---|---|
| **「沒有符合的料理」在轉盤區內** | ✅ 而且**上方沒有重複的卡片** |
| `ActionNotice`（淺色） | ✅ 染色卡底 + 1px 髮絲線邊框，**看得出是一張卡** |
| **`InfoNotice`** | ✅ 單行、無容器、不上色 |
| **產品規則第三條** | ✅ 實機文字是「**不夠 12 道，已放寬 什麼時候吃、菜系**」—— **維度名稱在** |
| 收合摘要 | ✅ 忌口全選時仍顯示「無牛・無豬・無海鮮・素可」 |
| 單元測試 | ✅ 43 個全過 |

### 分權重的效果實機看得出來

四個忌口全選那一屏，畫面上**只有轉盤區那一張卡片在喊**，沒有第二個東西跟它搶。
換成「已放寬」那一屏，提示縮成一行小字，**轉盤明顯往上移了** ——
設計說的「76pt 降到 20pt，轉盤位置幾乎固定」是真的，第一屏因此更完整。

這是 S3 唯一一處版面變化，而且它是分權重的副產物，不是為了改版面而改版面。

---

## 三、優先序：你自己點名的那一項，做對了

`isOverConstrained` 移走之後：

```
errorMessage → didFallBackToGeneric → relaxedDimensions → isShortOfSlots
```

**相對順序與移走前完全一致**，只是少了中間那一段。而且你把這件事寫進 `statusArea` 的
註解 —— 下一個人動這條鏈之前會先讀到。這比「我檢查過了」有用得多。

---

## 四、值得記的：你自己改掉了報告的寫法

> 以後表格只有兩種標記：✅ 實測過 與 ⚠️ 已實作、未實測，不會再用一個勾把兩者混在一起。

**而且這一份就是照這個規則寫的。** 你把「hero 區下拉關閉」列進「已實作、未實測」
並請我再跑一次 —— 那正是我退回你的那一項，你沒有因為改好了就打勾。

被退回之後改掉的是**做法**不是那一次的答案，這個差別很重要。

---

## 五、還沒實測的四項：我認可你的判斷

| 項目 | 我的處置 |
|---|---|
| 「找不到附近的店」的 `ActionNotice` | **不擋**。它與「沒有符合的料理」是同一個元件同一組參數，後者實測過 |
| 「改列一般餐廳」的新單行樣式 | **不擋**。舊卡片版我在土庫重現過，這次只換呈現；`InfoNotice` 本身實測過 |
| `reduceMotion` 降級 | **不擋**，但記在 S5 的深色逐頁驗收一起看 |
| 卡片優缺點的 `positive`／`negative` | S4 會重做候選清單，那時一起看 |

---

## 六、S3 結案

| 階段 | 狀態 |
|---|---|
| S3-1 結果頁架構（`.sheet` → overlay） | ✅ |
| S3-2 深色、轉場、角標 | ✅ |
| S3-3 狀態提示分權重、語意色 | ✅ |

**系統語意色在 App 裡已經沒有了**（`NoticeBox` 整個刪掉，`.orange`／`.blue`／`.green` 全退場）。
S1 刻意留到現在的那一項，這次清乾淨了。

`FilterBar`／`FoodPicker`／`FoodEditorView` **到 S3 結束仍是一行未改** —— 那是 S4。

---

## 七、Commit

放行。照你的建議一筆（補件兩項 + S3-3），理由成立：語意色與角標內距都碰 token 層。

**接著開 S4。** `Design/設計規格-篩選器與候選清單-v1.md` 我已核可
（見 `Design/PM核可-設計規格篩選器-v1.md`），裡面有三處要特別注意的產品規則紅線。

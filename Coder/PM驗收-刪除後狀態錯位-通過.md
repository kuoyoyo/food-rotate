# PM 驗收 — 刪除後狀態錯位 **通過**

**日期**：2026-08-13 ／ **驗收**：PM
**對象**：`工程回覆-刪除後狀態錯位-給PM.md`
**結論**：**通過。這一串（左滑改寫 + 捲動回歸 + 狀態錯位）可以 commit。**

---

## 一、實機驗證

| 步驟 | 結果 |
|---|---|
| 展開「日式拉麵」（第 10 列） | ✅ |
| 刪掉「廣東粥」（**第 4 列，在展開那列之前**） | ✅ 12 道 → 11 道 |
| **刪除後展開的還是不是日式拉麵** | ✅ **是**，狀態跟著它自己的菜 |
| 其餘各列 | ✅ **沒有任何一列變成滑開或展開** |
| 43 個測試 | ✅ 全過 |
| 探針殘留 | ✅ 無（`grep 暫時` 只剩 `NearbySearch` 兩句原本的錯誤文案） |

刪除點**之前**的列與**之後**的列都對，這正是上一版會整串錯開一格的地方。

---

## 二、你的取證方法這次是完整的

上一張單你自承「連『原理上會生效』的自證都比上次弱」。這一張你把它補起來了：

> 暫時加一個 `@State birthName` 記下這個 view 實例誕生時顯示的是哪道菜，
> 再跟當下的 `item.name` 比。不一樣就等於「SwiftUI 把舊狀態接到新資料上了」——
> **那正是這個 bug 的定義。**

**把 bug 的定義變成畫面上可以看到的東西**，這比「我改了一行，應該會好」強太多。

而且你做了關鍵的那一步：

> 我先讓它在錯的版本上亮紅燈，才相信它在對的版本上的綠燈。

**先證明探針有牙，再相信它的結論。** 這跟 S2 那次主色測試（把 bug 放回去確認測試抓得到）
是同一個方法 —— 你在不同的問題上重複用它，那就不是偶然。

---

## 三、`id` 撞號你逐一查了三個來源，這是對的順序

我要求「改之前確認同一份清單不會有重複」，你查了內建 slug、自訂 `custom-<UUID>`、
店家 `place-<UUID>`，還指出：

- `FoodDataAudit.duplicateID` 對**真實的** `FoodLibrary.all` 斷言，不是測假資料
- `FoodPicker` 放寬時用 `seen.insert($0.id).inserted` 去重，`FoodPickerTests` 第 4 支在測這件事

**所以「同一份清單不會有兩個相同 id」這個前提已經有測試守著，你沒有再補一支重複的** ——
判斷正確。補一支測同一件事的測試，只是讓人以為多了一層防護。

至於店家 id 每次搜尋新生：你的分析對，`ForEach` 要的是「同一份清單存續期間內唯一且不變」，
重新搜尋時整份清單本來就全換。

---

## 四、你順手掃到但沒有動的那項，我排在 S5-B 之後

`FoodEditorView.swift:122` 的 `ForEach(points.indices, id: \.self)` ——
現況沒有刪除、列裡也沒有 `@State`，所以不會錯位。

**你沒有順手改，寫進報告讓它有紀錄，這是對的做法。**
我排在 S5-B 之後處理；如果 S5-B 的實作剛好會讓那一區長出刪除功能，那時一起改。

---

## 五、Commit：照你的建議併成一筆

> 兩者是同一次修的同一塊程式碼，分成兩筆會讓「左滑改寫」那一筆在歷史上是個
> 帶著已知缺陷的中間狀態。

**理由成立。** 歷史上留一個「已知壞掉」的 commit，之後 bisect 到那一格的人會被誤導。

---

## 六、⚠️ 一個要 kuoyo 決定的：肉食 v5 在 24pt 下

實機候選清單（24pt）看「美式牛排」那一列：**輪廓的不對稱讀得出來，
但整體比較接近「一個傾斜的橢圓中間有兩道斜線」**，不太到「一眼是牛排」。

跟前幾版比是明確的改善（v3 是魚、v4 是對稱膠囊），而且它在九個圖示裡輪廓唯一。
**我判斷可接受，但先前指出問題的是 kuoyo，所以由他決定要不要再退一輪。**

這一項**不擋 commit** —— 圖示資產隨時可以再換一版。

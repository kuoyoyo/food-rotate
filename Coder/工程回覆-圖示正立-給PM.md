# 工程回覆 — 圖示正立已實作（給 PM）

**日期**：2026-08-11 ／ **提出**：程式設計師
**依據**：`Design/設計規格-圖示與動效-v1.md` 三-3（21:59 版）
**狀態**：完成。未 commit

---

## 〇、先認漏接

「設計決定要就說一聲」那句是我沒重讀 21:59 更新的規格就寫的（我的報告 22:00）。
**同一種漏接第二次了**，前一次是 `Design/icons/` 的 SVG。

已經把它變成動作，寫進我的長期記憶，不是靠這次記得：

> 報告裡每一句「還沒有」「還在等」「對方沒說」，送出前都要有一次**當下**的查證支撐。
> 查證要含：`git status`、進子資料夾（不能只 `ls` 根目錄）、依時間找新檔、
> **重讀規格本身**而不是憑上次讀到的版本。
> 動手前也要確認手上的規格與資產是最新版。

這次就吃到紅利：查的時候發現 `form-rice.svg` 在 21:57 也更新了，我 imageset 裡是舊複本，
**9 個 SVG 全部重新匯入**了。新的飯食圖示已經在實機畫面上（碗上面幾顆飯粒，不再是 Ø）。

---

## 一、實作

照規格的公式，兩層旋轉都扣：

```
圖示自身旋轉 = -(drawAngle + 轉盤當前角度)
```

做法是在畫圖示前，從那一格的 layer 再分出一個子 layer：先平移到圖示位置，
再反向轉回來。菜名不受影響，仍然是徑向的。

**全程抵消，不是只在靜止時轉正** —— 照規格的理由，後者會在停止瞬間跳一下。

---

## 二、驗證

### 靜止（12 格）

九種圖示全部水平正立，菜名仍是徑向。實機確認到的：

- 麵食 = 碗 + 斜插筷子・飯食 = 碗 + 飯粒・小吃 = 竹籤・麵包餅皮 = 楔形・湯的 = 湯匙・肉食 = 帶骨肉
- **新的飯食圖示看得出是一碗飯了**，Ø 的問題沒有了
- 一盤裡出現三、四個飯食圖示 —— 這是對的，圖示只表達類型

### 轉動中

**抓了轉動中的畫面：菜名各種角度、中心鈕已淡出，圖示全部維持水平。**
全程抵消確認成立，不是只有停下來才正。

### 順帶確認主色沒有再退回

同一張截圖：「開始轉」按鈕、指針、中心鈕外環、「換一組」全部是橘的。

| 項目 | 結果 |
|---|---|
| 單元測試 | ✅ 35 個全過 |
| 建置 | ✅ |
| 12 格靜止 | ✅ 圖示正立、零截斷 |
| 12 格轉動中 | ✅ 圖示正立 |
| 主色 | ✅ 橘 |
| 模擬器設定 | ✅ 已還原（格數、autoSpin） |

---

## 三、規格三-3 提到的退路，我的看法

> 「若實機上看起來不對，退路是只在 10／12 格拿掉圖示 —— 但請先看過正立版再決定。」

**看過了，我認為不需要動用退路。** 正立之後辨識度差很多：轉了的線稿本來是一堆亂線，
正立之後碗、湯匙、竹籤、楔形一眼可分。12 格下圖示仍然偏小（框約 18.7pt、
圖形約佔六成），但**現在小的是尺寸，不是認不出來**，跟之前是兩回事。

這仍然是設計的判斷，我只提供實機所見。截圖可以直接看。

---

## 四、Commit

計畫不變，兩筆（資產一筆、程式一筆）。資產那筆現在包含 21:57 更新的 `form-rice.svg`。
程式那筆的訊息補一句：

```
圖示保持正立：畫圖示時把該格角度與轉盤角度都反向扣掉，全程抵消而不是只在
靜止時轉正——後者會在停止瞬間跳一下。線稿不像 emoji 耐轉，emoji 靠質地與
顏色辨識，線稿靠輪廓方向，一個碗倒過來就不是碗。菜名仍然是徑向的。
```

S2 這邊工程的部分我這裡沒有剩下的了，等你驗收。

# PM 驗收 — S3-1／S3-2 結果頁：**大部分通過，還差一項**

**日期**：2026-08-12 ／ **驗收**：PM
**對象**：`工程回覆-S3結果頁-給PM.md`
**結論**：**架構與視覺全部通過。下拉關閉的可拖曳範圍比你報的窄，補完就放行。**

---

## 一、你說沒辦法測的三條路徑，我測了

你列的 A 案（我在實機上跑）是對的選擇 —— 我有模擬器的觸控工具，這三條我逐一跑過。
**不用加 UI test target，也不用在我機器上裝東西。**

| 路徑 | 方法 | 結果 |
|---|---|---|
| 「完成」按鈕 | 實機點按 | ✅ 關閉，回到轉盤，中選格高亮還在 |
| 開「附近的店」 | 實機點按 | ✅ 開得起來，**而且是淺色的** —— 深色只到 `ResultSheet` 的界線成立 |
| 下拉關閉（標題列） | 實機拖曳 420pt | ✅ 關閉 |
| **下拉關閉（hero 區）** | 實機拖曳 405pt，慢速 | ❌ **沒有關閉** |

---

## 二、🔴 唯一的阻擋項：hero 區不吃拖曳

### 規格怎麼寫的

`設計規格-結果頁-v1.md` 四-2：

> 可拖曳區域 | **整個標題列 + hero 區**；優缺點卡片區不吃拖曳（那裡要能捲動）

### 實測

我試了兩次，都是從 hero 圖示往下拖：

| 次 | 起點 | 位移 | 速度 | 結果 |
|---|---|---|---|---|
| 1 | hero 圖示上緣 | 420pt | 快 | 沒關 |
| 2 | hero 圖示 | 405pt | 慢（每段 100ms） | 沒關 |

門檻是 120pt，兩次都遠超過。**同一組手勢從標題列拖就會關**，所以不是我的手勢有問題，
是拖曳只掛在標題列上。

### 為什麼這件事要退回

你回報寫的是：

> 下拉關閉（**完整版**：捲到頂再往下拉才關） | ✅ 已實作

而實際的行為**比設計給的退路還窄** —— 退路是「只有標題列**與 hero** 可拖曳關閉」，
現在是「只有標題列」。

**這一項本身是小事**（gesture 的掛載範圍），但「報成完整版、實際不是」比範圍窄本身更值得講。
你自己在第四節寫得很好：

> 「寫好」跟「實測過」是兩回事，我不會把它報成後者。

這句話的標準要一路貫徹到表格裡 —— 第一節那個 ✅ 應該是「已實作，未實測」。
**你沒辦法實測不是問題，把未實測的東西打勾才是。**

---

## 三、✅ 通過的部分

| 項目 | PM 實測 |
|---|---|
| 建置與單元測試 | ✅ 43 個全過 |
| 深色結果頁版面 | ✅ 與規格第二節結構圖一致 |
| **系統淺色下結果頁仍是深色** | ✅ 壓暗層透出淺色背景，結果頁本身是深的 |
| 分頁列／導覽列被蓋掉 | ✅ 掛到 `RootView` 是對的 |
| **主要按鈕是深墨字** | ✅ 沒有改回白字 |
| 角標「台」「小吃」／「美」「飯食」 | ✅ 兩個都在 |
| hero 圖示 76pt | ✅ 飯食的碗與飯粒在這個尺寸很清楚 |
| 優點綠／缺點橘 | ✅ 已用 token |
| 拿掉 `navigationTitle` | ✅ |

### 兩處偏離派工單，都判定正確

**1. 結果頁掛在 `RootView` 不是 `RotateView`** —— 我指的位置是錯的。
`.sheet` 是系統層呈現、自動蓋掉全部；overlay 只蓋得住它所在那一層。
你照我寫的做，分頁列會壓在結果頁上面。**你發現了並且說明了為什麼**，這是對的處理。

**2. `matchedGeometryEffect` 的 `isSource` 要跟著開關切換** —— 規格沒寫，
不做的話 hero 會被永遠釘在轉盤那一格的框上、直接壓在菜名上。

這一段值得留著：兩端同時存在（結果頁只壓暗 0.55，轉盤還在後面），
所以 source 必須換邊。還有 `Canvas` 畫的東西不能參與 `matchedGeometryEffect`，
中選那格改用真的 `Image` 疊上去。**這兩個都是規格想不到、只有做下去才會遇到的。**

---

## 四、順帶：角標內距設計已經定了

定案 **8**（`space8`），不是 6。理由：間距階梯是 4 的倍數、沒有 6；
16pt 高的角標配 8pt 水平內距比例正確。規格已更正（第六節 + 更正註記）。

`TagBadge.horizontalPadding` 改一個常數即可。

---

## 五、修完就放行

1. hero 區加進可拖曳範圍
2. 角標水平內距 6 → 8

兩項都是小改。修完我再跑一次那三條路徑，通過就一起 commit。

`Design/設計規格-狀態提示與語意色-v1.md`（S3-B）我已核可
（見 `Design/PM核可-設計規格狀態提示-v1.md`），你接著做 S3-3 不必等。

# 專案現況

**最後更新**：2026-08-27 ／ **維護**：PM
**這是唯一的狀態入口。** 其他文件（README、各角色資料夾）的進度段落一律指向這裡，
不要在兩個地方各寫一份 —— 那正是它們上次過期的原因。

---

## 現在在做什麼

**2026-08-27 全專案稽核收批。版本 1.1.0 (2)。**
報告：[`稽核報告-2026-08-27.md`](稽核報告-2026-08-27.md)。

這一輪跟前兩輪 QC 問的又是不同方向的問題，三者要分開看：

> **S6 問「這個結果屬於哪一次請求」（時機）。
> S7 問「模式換了，上一個模式留下來的狀態誰負責清」（歸屬）。
> 這一輪問「我們宣稱有的東西，實際上有沒有」（對照）。**

抓到 10 項，修 7 項。最值得記的一項：

| | |
|---|---|
| **`素可` 這個忌口標籤在 50 道菜裡零命中** | 忌口是硬條件、永遠不放寬，所以點下去**必定**空轉盤，畫面還請他「取消其中一個忌口」——而他取消的就是他唯一想要的那個條件 |
| 為什麼三道防線都沒攔到 | `FoodDataAudit`、`FoodDataAuditTests`、`FoodPickerTests` **都在問「這道菜有沒有標籤」，沒有人反過來問「這個標籤有沒有菜」** |
| 防線 | `FoodDataAudit.restrictionMatchesNothing`（只驗忌口；軟標籤零命中只是會被放寬，不是死路） |

**移除一個 case 的連帶風險比缺陷本身更大**：`FoodItem.tags` 原本是合成 `Codable`，
兩條解碼路徑都是 `try?`，所以只要使用者有任何一道自訂料理掛了「素可」，
**他全部的自訂料理會在升級後靜靜消失**。已改成寬容解碼（不認得的標籤丟標籤，不丟菜），
`TagMigrationTests` 五條，驗過紅→綠。

| 項目 | 內容 | 狀態 |
|---|---|---|
| A（P1） | 「素可」零命中，選了必定空轉盤 | ✅ 移除 + 補防線 |
| B（P1） | 移除 case 會讓自訂料理整份消失 | ✅ 寬容解碼，先寫測試再改 |
| C（P2） | 轉動中改標籤沒被擋（本表下方掛了很久的那一項） | ✅ 已修，擋在 UI 層，model 一字未動 |
| D（P3） | 歷史頁空的時候不顯示降級提示 | ✅ 已修 |
| E（P3） | 「以後都不要的」清單不套改名 | ✅ 已修 |
| F（P3） | `DishListRow<EmptyView>.separatorInset` | ✅ 搬進 `DishListRowMetrics` |
| G（P2） | **主色兩份真相，兩份都叫「醬」**（本表原本沒記到） | ✅ 收成單一來源，畫面零變化 |
| — | Widget 拿不到 token | ✅ 已解（動了 pbxproj，沒搬檔案） |
| H | `category` 混了吃法值 | ⏸ 依 2026-08-11 裁示不動，但**現在看得見了** |
| I | 缺 `PrivacyInfo.xcprivacy` | ⏸ 等 kuoyo 送審決定 |

### 一件要記著的事：「警告 0」以前是量錯範圍的

本檔原本寫著「全新 derivedData 建置 Swift 警告 0」。
**那句話是用 `xcodebuild build` 量的，而那個指令根本不編測試 target。**
改用 `xcodebuild test` 量，進場時測試 target 有 3 個警告，從 S6 掛到現在。

已清掉。以後要宣稱「警告 0」，**請用 `xcodebuild test` 量**，不要用 `build`。

### 菜色資料庫（kuoyo 指定交付）

50 道菜以前只存在於 `foods.json`，GitHub 上看不到內容。
現在有 [`docs/菜色資料庫.md`](docs/菜色資料庫.md) —— 全部 50 道、250 則優缺點全文、
標籤覆蓋率表。**由 `Tools/make-food-catalog.swift` 產生**，跟 App 編同一批 Core 型別，
所以不會有一份說法留在文件裡過期。刪掉重跑 `git diff` 必須是空的。

### S6 硬化收批（已結束）

依 `QC專案健康檢查報告-2026-08-16.md`。規劃與裁示在 `PM/PM規劃-S6依QC報告.md`，
派工在 `Coder/PM派工-S6-工程.md`。

| 批次 | 內容 | 狀態 |
|---|---|---|
| 第一批 | 非同步邊界（P0-1、P1-1、P1-2、P1-3） | ✅ **完成**（`4c44fea`） |
| 第二批 | 資料正確性與送審（P1-4、P2-1～P2-4） | ✅ **完成**（`5b85755`）；P1-5 等 kuoyo |
| 第三批 | 回歸防線（P2-5、P3-1、P3-2） | ✅ **完成**（`4c940c9`）；P3-1 收在目前位置，不開 UI test target |
| 設計（並行） | P3-2 設計端：圖示渲染腳本 | ✅ 完成（`5ee60b8`） |

---

## 已完成

| 階段 | 內容 | 合併 |
|---|---|---|
| S1 | Design tokens、Theme、資料稽核 | `5424285` |
| S2 | 轉盤兩行排版、圖示系統、中選動效 | `f2ca550` |
| — | 「去哪吃」問題單：菜系降級為搜尋詞、誠實退回 | `604c860` |
| S3 | 結果頁重做、狀態提示分權重、語意色 token 化 | `2838110` |
| S4 | 篩選器網格、候選清單高密度列 | `4be5e5e` |
| S5-A | App Icon 正式版、轉盤白邊結案、二次確認 | `504951e` |
| S5-B | 其餘四頁套 token | `a5c120d`（PR #1） |

**視覺與體驗改版到 S5-B 為止全部完成。**
`FoodPicker.swift` 從 S1 到 `901849d` **一行未改**；QC 2026-08-19 的 F 動了它，
但動的是**回報**（`emptyReason` 分辨兩種空），不是抽樣 ——
**八條產品規則行為仍然零變化**，`FoodPickerTests` 那兩條不可改規則原樣全過。

---

## 已知問題

以稽核報告為準，逐項狀態在派工單裡追蹤。
最新一輪是 [`稽核報告-2026-08-27.md`](稽核報告-2026-08-27.md)（狀態整理在最上面那一節）。

### 2026-08-19 這一輪（`QC稽核報告-2026-08-19.md`）

| 嚴重度 | 項目 | 狀態 |
|---|---|---|
| **P1** | 退回一般餐廳的提示跨模式殘留，且蓋掉「已放寬」——產品規則第三條失效 | ✅ 已修（`clearNotices()` 收成單一歸零點） |
| **P1** | 從歷史還原時 `source` 不切、`errorMessage` 不清 | ✅ 已修（先切模式再放清單，順序是正確性） |
| **P1** | 「去哪吃」模式調大格數會抽菜色蓋掉店家 | ✅ 已修（補格加 `source == .dishes`） |
| P2 | 設定頁的格數 Picker 直接寫 `AppSettings`，繞過重設與補格 | ✅ 已修（UI 層只剩 `RotateViewModel.wheelSlots` 一條路；兩個 Picker 轉動中停用） |
| P3 | `MyListView` 多選刪除時索引錯位 | ✅ 已修（收進 `CustomFoodStore.exclude(atOffsets:)`）。**實測 `IndexSet(0..<3)` 會 trap**，不只是刪錯人 |
| P3 | 候選池被排空時說「忌口把選項篩掉了」 | ✅ 已修（`PickResult.emptyReason` 分兩種空，出口各自指對地方） |
| 外觀 | 歷史頁的圖示還是 emoji | ✅ 已修。**S5-B 那條「這兩頁一律保留 emoji」的決定套錯頁了** —— 規則是「不能換掉使用者的內容」，而歷史頁那顆 emoji 是 `foods.json` 裡我們自己寫的。依據改成「這是誰的東西」：歷史＝線稿、我的清單＝emoji |

**目前測試支數：109 支 / 22 suites**（S6 收批時是 80 / 16，S7 是 99 / 21）。
全新 derivedData **`xcodebuild test`** Swift 警告 0 —— 增量建置沒噴不算數，
**只用 `build` 量也不算數**（那不編測試 target，見上面那一節）。

### 2026-08-16 那一輪（`QC專案健康檢查報告-2026-08-16.md`）

| 嚴重度 | 項目 | 狀態 |
|---|---|---|
| **P0** | 轉盤完成 Task 未取消，可能崩潰 | ✅ 已修 |
| **P1** | 搜尋過期結果回灌、載入中新條件被忽略 | ✅ 已修 |
| **P1** | `placemarkNotFound` 跳過 fallback | ✅ 已修 |
| **P1** | 餐廳歷史還原是死按鈕 | ✅ 已修（只展示不還原，不加任何視覺差別） |
| **P1** | 缺 `PrivacyInfo.xcprivacy`（上架硬門檻） | ⏸ **等 kuoyo 確認送審狀況**，不能靠猜 |
| P2 | 距離文案寫死十五公里、地圖視窗固定 2.5km | ✅ 已修（地圖視窗未親驗，留第三批） |
| P2 | 自訂料理改名有兩套真相來源 | ✅ 已修（實機驗過兩頁一致） |
| P2 | 名稱被當成 winner 身分 | ✅ 已修（winnerID） |
| P2 | SwiftData 降級靜默 | ✅ 已修（`HistoryStorage`：`try?` 收斂到單一入口＋非阻擋提示，實機驗過） |
| P3 | 狀態層無自動測試 | ✅ **收批**：43 → 80 支，六個注入點齊。UI test target 裁示不開，換成 `PM/實機回歸檢查表.md` |
| P3 | 驗收證據不可重現（工程、設計、PM 三邊都是） | ✅ 設計端渲染腳本、工程端 80 支測試、PM 端 `實機回歸檢查表.md` |

### 掛著但不擋任何人的

2026-08-27 清掉四項（下方「已清掉的」記著它們去哪了）。剩下的：

| 項目 | |
|---|---|
| 「肉食」圖示在 24pt 下辨識偏弱 | kuoyo 已拍板接受 v5 |
| `FoodEditorView` 的 `ForEach(points.indices)` | 現況沒有刪除功能所以不會錯位，加了就會 |
| `foods.json` 的 `category` 混了「輕食」「鍋物」兩個吃法值（四道） | `PM/PM提案-7道菜補標籤-待核可.md` 第四節 2026-08-11 裁示不動（改它是視覺變更）。**2026-08-27 追記：它現在看得見了** —— `docs/菜色資料庫.md` 會在那四道旁邊標「分類寫「鍋物」」，不再只是一句記在提案裡的話 |

### 已清掉的（2026-08-27）

| 項目 | 怎麼解的 |
|---|---|
| `DishListRow<EmptyView>.separatorInset` 讀法彆扭 | 搬進檔案層級的 `nonisolated enum DishListRowMetrics`，順帶收掉 body 裡的兩個魔術數字。跟 `DishRowArt` 是同一個根，現在這個檔案裡一致了 |
| 歷史頁**空的時候**不顯示降級提示 | 舊理由「空清單時沒有東西會失去」**看錯了時態** —— `isEphemeral` 講的是接下來存不住，而空歷史的人正是下一筆就要吃虧的那一個 |
| Widget 拿不到 token | **沒有搬 `Shared/`**：加一個 `PBXFileReference` + 只掛給 Widget 的 `PBXBuildFile`，檔案留在原地，路徑與所有文件引用都不用改。兩個 target 的 SwiftFileList 各驗過 |
| **轉動中改篩選標籤**沒有被擋 | 已裁示並修。三處補 `.disabled(isSpinning)`，**擋在 UI 層、`RotateViewModel` 一字未動**（在 model 加 guard 會拆掉 `WheelSpinnerRaceTests` 重現 P0-1 的路）。模擬器驗過靜止／轉動中／轉完還原三態 |

---

## 一筆要記的帳

`6757733`（訊息寫「文件：PM 文件收進 PM/，新增專案 README」）**夾帶了當時進行中的
S5-B 程式碼** —— `DishListRow.swift`、`HistoryView.swift`、`FoodEditorView.swift`、
`make-icon.swift`。

那是 **PM 用 `git add -A` 沒有先看 `git status` 造成的**，不是工程下的指令。
歷史不改寫（理由與用 `revert` 修 README 相同：保留歷史比抹掉乾淨重要），在此記明。

---

## 文件配置

| 位置 | 內容 |
|---|---|
| `Design/` | 設計規格、視覺提案、圖示原始檔（七份規格全部結案） |
| `Coder/` | 工程交接、實作回報、PM 驗收與問題單 |
| `PM/` | 跨角色的統整、裁示與規劃 |
| `docs/` | `菜色資料庫.md`（**產生的**，見 `Tools/make-food-catalog.swift`）、`screenshots/` |
| 根目錄 | `README.md`（專案介紹）、本檔（狀態）、三份稽核報告（`2026-08-16`／`2026-08-19`／`2026-08-27`）、`大改紀錄`／`DEVLOG`（沿革） |

**八條產品規則的正本**在 `專案現況彙整-給設計師.md` 第六節 —— 名字是給設計的，
但兩邊都在讀。

---

## 維護規則

**每次 PM 驗收通過，更新這份文件才算完成。** 這是 Definition of Done 的一部分，
不是「有空再做」。上一次入口文件過期，讓 QC 在報告裡列了一條 P2 —— 那是可以避免的。

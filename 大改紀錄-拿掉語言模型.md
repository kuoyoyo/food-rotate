# 大改紀錄：拿掉語言模型

**日期**：2026-08-11
**範圍**：從「語言模型產生菜單」改成「內建資料庫 + 標籤篩選」，並加上自訂料理、Google 地圖、附近餐廳轉盤
**狀態**：三階段全部完成，模擬器（iPhone 17 Pro）實跑驗收通過

這份文件記錄這次改動的**原因與取捨**。逐檔的細節在程式碼註解裡，這裡只寫「為什麼」。
原本靠模型的那段歷史仍留在 [DEVLOG.md](DEVLOG.md)，兩份不重複。

---

## 一、為什麼要改

原本的流程是：使用者打一段中文 → `EngineRouter` 送給 Apple 裝置模型或自架的
OpenAI 相容服務 → 模型回一批菜 → 程式再把它修到能用。

問題不在模型不好，在於**這件事本來就不需要模型**。「台灣有哪些常見主食、哪些是日式、
哪些不含牛」是一組穩定的事實，不會每次問都不一樣。用模型去產生它，換來的是：

| 成本 | 具體是什麼 |
|---|---|
| 一輪要等 18–25 秒 | 裝置模型實測 18 秒、自架 25 秒 |
| 需要一整套掩飾等待的機制 | Live Activity 進度條、階段回報、超時換文字 |
| 需要三層事後補救 | `DietaryFilter`、`MainDishFilter`、`normalized()` |
| 需要設定 | 自架服務位址、模型名稱、API key、fallback 開關 |
| 行為不可預測 | 同一段 prompt 跑兩次結果差很多 |

最刺眼的是補救層。`DietaryFilter` 的檔案註解裡寫著：輸入「清爽一點」，
第一輪回鹽酥雞、雞排、煎餅；把規則寫進 prompt 並點名「煎餅不行」之後，
改成回麻辣海鮮湯、辣炒蝦仁、蚵仔煎。**連續三輪都只對一半**，
所以只好在程式層再用關鍵字把明顯不對的濾掉。

那是在用一層猜測去修另一層猜測。改成查表之後，這件事從「希望模型照做」變成「查得到就是查得到」。

## 二、改完之後

- **抽一組候選是同步的、瞬間的、不會失敗。**
- **選了「無牛」就一定不會出現牛肉麵**，不是「模型應該不會給」。
- 「吃什麼」模式完全離線，不需要網路、定位或任何金鑰。
- `-3000` 行左右的模型相關程式碼消失，換成一份 50 筆的 JSON 加一個純函數。

刪掉的檔案：

```
FoodRotate/Engines/                  整個目錄（EngineRouter, AppleEngine,
                                     OpenAICompatEngine, FoodSuggesting）
FoodRotate/Models/DietaryFilter.swift     關鍵字濾掉不符飲食限制的菜
FoodRotate/Models/MainDishFilter.swift    濾掉配菜小菜
FoodRotate/Models/PointSanitizer.swift    清掉模型寫的空話優缺點
FoodRotate/Models/TextNormalizer.swift    簡繁轉換、外語字元偵測
FoodRotate/Models/DemoMenu.swift          「不呼叫模型也能截圖」的假資料
FoodRotate/Services/KeychainStore.swift   只用來存 API key
FoodRotate/Views/PromptBar.swift          自由文字輸入框
```

新增的：

```
FoodRotate/Core/FoodItem.swift        純資料型別，不再有 @Generable / @Guide
FoodRotate/Core/FoodTag.swift         六個維度的標籤，篩選唯一的語彙
FoodRotate/Core/FoodPicker.swift      抽樣，純函數
FoodRotate/Core/FoodLibrary.swift     從 bundle 載入 foods.json
FoodRotate/Core/foods.json            50 道
FoodRotate/Views/FilterBar.swift      標籤篩選器
FoodRotate/Views/FoodEditorView.swift 新增／編輯自己的料理
FoodRotate/Services/CustomFoodStore.swift  自訂、排除、改名的儲存
```

## 三、關鍵決策

### 3.1 為什麼資料是 JSON 而不是 Swift 字面值

因為**之後要移植 Android**。`foods.json` 可以原封不動丟進 `assets/`，
用 `kotlinx.serialization` 解同一份，不必重打一次五十道菜。

同樣的理由讓 `Core/` 這個目錄存在：裡面只用 `Foundation` 的基本型別，
不碰 SwiftUI、SwiftData、`@Observable`。移植時的界線是「搬 Core、重寫其他」，
一眼看得出來。

**沒有**為了移植做抽象層或引入跨平台框架。這是 iOS 專案，Android 是之後的事，
現在該做的只是不要製造沒必要的移植債。

### 3.2 分類與標籤刻意分成兩件事

舊的 `FoodItem.categories` 把「日式」跟「麵食」放在同一個清單裡，一道菜只能選一個，
於是拉麵要嘛是日式要嘛是麵食。

改成：`category` 是**菜系**（12 類，拿來顯示），`tags` 是**六個維度**（拿來查）。
「想吃日式的麵」才問得出來。

| 維度 | 值 |
|---|---|
| 忌口 | 無牛、無豬、無海鮮、素可 |
| 菜系 | 台式、中式、日式、韓式、東南亞、南亞、義式、美式、歐陸、墨西哥 |
| 吃法 | 麵食、飯食、湯的、鍋物、小吃、麵包餅皮、輕食 |
| 什麼時候吃 | 早餐、午餐、晚餐、宵夜 |
| 口味 | 清爽、重口味、辣、偏甜 |
| 其他 | 便宜、快、吃得飽、健康 |

代價是菜系標籤跟 `category` 重複存了一份。這是刻意的，用途不同。
新增料理時兩者會自動連動（選了分類「義式」就自動掛上義式標籤），
否則使用者選了分類卻篩不到，那是最容易漏的一個坑。

### 3.3 忌口是正面標記，不是負面排除

`foods.json` 裡標的是「這道菜**不含**牛」，不是執行期用關鍵字猜「牛肉麵含牛」。

人寫死的標記比關鍵字可靠得多，也不會像舊的 `DietaryFilter` 那樣把「牛蒡天婦羅」誤殺。

**忌口永遠不會被自動放寬。** 湊不滿格數時寧可轉盤只有四格，
也不能讓不吃牛的人看到牛肉麵。其他維度（菜系、口味…）才會放寬。

### 3.4 放寬條件是「往下補」，不是「重來」

這是實跑時抓到的 bug，值得記一筆。

一開始寫成：候選不夠就整組換一批寬鬆的條件重篩。結果選「日式 + 無牛」時，
標了無牛的日式只有 5 道、轉盤要 6 格，菜系被放寬之後——
**轉盤上一道日式都沒有**。使用者明明講了想吃日式。

改成逐層累積：完全符合的先全部放進去，不足的才從放寬一層的結果裡補。
現在那 5 道日式一定在盤上，只有第 6 格是別的菜系。

同一輪還修掉另一個相關的錯：`pick()` 為了「格數往上調時不用重抽」而一律要 12 道，
於是放寬的判斷也用 12 當基準，明明剛好 6 道符合、轉盤也只有 6 格，
畫面卻寫著「完全符合的不夠，已放寬菜系」。少抽幾道的代價只是格數調大時重抽一次，
拿它換一句不實的提示不划算。

### 3.5 優缺點：保留，但改成人寫

kuoyo 提供的 [taiwan_main_dishes_50.txt](taiwan_main_dishes_50.txt) 每道都附了優缺點，
角度是營養學（鈉、飽和脂肪、GI 值、Omega-3）。直接搬進去有兩個問題：
50 道讀起來像同一份衛教單張，而且「決定現在吃什麼」時鈉含量幫不上忙。

改寫規則沿用舊 `FoodItem.swift` 註解裡實測歸納出的那幾條——現在改由人寫，就真的做得到：

- 拆成 2–3 則優點、1–2 則缺點，每則 25 字內
- **保留一則原本的營養判斷**（那是這份資料的價值）
- 至少一則優點要換成決策資訊：份量、價位、多久吃得到、什麼狀態適合吃
- 不寫「鮮嫩」「美味」「營養豐富」這種換哪道菜都成立的話
- 缺點只講這道菜本身，不講排隊、服務、裝潢

以牛肉麵為例：

```
來源  優點：蛋白質豐富、牛肉富含鐵質與鋅
      缺點：湯頭鈉含量高（特別是紅燒），容易水腫與增加心血管負擔

改寫  pros: ["一碗就飽，不用再配小菜", "牛肉補鐵，運動後很適合", "巷口麵店幾乎都找得到"]
      cons: ["紅燒湯頭偏鹹，隔天易水腫", "一碗常要 180 元起跳"]
```

### 3.6 篩選器預設收合

六個維度攤開來佔掉整個第一屏，一開 App 看不到轉盤。轉盤才是主體，所以預設收起來，
收起時那一行顯示目前的條件摘要（例如「日式・無牛」）而不是一個空的「篩選」按鈕——
使用者要知道眼前這組是在什麼條件下抽出來的，否則會以為 App 隨便給。

### 3.7 Live Activity 從「等模型」改接到「找店」

菜色改成本地查表之後，「吃什麼」是瞬間的，硬要在島上顯示進度條就是演戲。
但新增的「去哪吃」模式真的會讓人等（定位 + `MKLocalSearch`，數秒），
所以整組 Live Activity 搬過去，階段換成那條路徑真正會發生的事：

```
舊  thinking / retrying / fallback(engine:) / overrun / done / failed
新  locating / searching / done(count:) / failed
```

進度條與文字的來源仍然刻意分開：進度條是時間估計（拿不到真實百分比），
文字是真實階段。兩者都不需要說謊。

### 3.8 店家資訊：MapKit 給得出什麼、給不出什麼

kuoyo 要求「讓使用者知道店家有沒有營業」。查過 iOS 26 SDK 的 `MKMapItem.h`，
**MapKit 沒有營業時間，也沒有開／關狀態**。公開屬性只有：

```
name / location / address / addressRepresentations
phoneNumber / url / timeZone / pointOfInterestCategory / identifier
```

要真的知道開沒開必須接 Google Places API，那需要自己的金鑰與計費，
移植 Android 時還要再一把。決定先做 MapKit 給得出來的，並且**不假裝知道**：

- 卡片副標改成「類別 · 距離」（餐廳 · 258 公尺、咖啡廳 · 307 公尺）
- 展開後列出完整地址、電話、網站
- 給一顆「打電話問」按鈕直接撥號 —— 想確認有沒有開、要不要訂位，打過去問最快

`MKPointOfInterestCategory` 只翻這個 App 搜得到的那幾類（餐廳、咖啡廳、烘焙坊、
食品市場、啤酒吧、酒莊），其餘回 `nil` 讓 UI 省略。顯示一個沒翻譯的英文列舉值
比不顯示更糟。

### 3.9 導航用店名 + 地址，不用經緯度

原本 Google 地圖那條路傳的是 `destination=25.0402,121.5628`。座標雖然精準，
但使用者在 Google 地圖上看到的是一個**沒有名字的圖釘**：點不到評論、營業時間、照片，
也沒辦法存進自己的清單或分享給別人。

改成 `destination=<店名 空格 地址>`。實測導向「鼎泰豐 A4店 110臺北市信義區…」，
落地的是真正的店家頁面。地址一定要一起帶，否則連鎖店（鼎泰豐、麥當勞）會導到別家分店。

Apple 那條路本來就帶 `MKMapItem.name`，維持不變。

### 3.10 距離上限改成可調，預設 5 公里

原本寫死 15 公里。那對「現在要去吃飯」太遠了——開車半小時的店不會是今天午餐的選項。
改成 1／3／5／10 公里的選單，放在「去哪吃」模式的篩選區（「吃什麼」模式不顯示），
選擇會記住。

上限唯一真正生效的地方是搜尋後的 `filter`，因為 `MKLocalSearch.Request.region`
對 MapKit 只是建議不是硬性條件（原本就有這個註解，實測會回日本的店）。
`region` 現在跟著使用者選的值走，乘二是因為它是邊長不是半徑。

### 3.11 店家借用 `FoodItem`（已知的技術債）

「去哪吃」模式要把店家放上轉盤，而轉盤、卡片、結果頁、歷史四個地方都吃 `FoodItem`。
目前的做法是讓 `NearbyPlace` 轉成 `FoodItem` 的形狀：

```
id       = "place-<UUID>"
category = 「類別 · 距離」（原本是菜系）
pros     = [地址, 電話, 網站]（原本是優點）
```

好處是四個地方一行都不用改；代價是欄位語意被撐開了。UI 用 `FoodItem.isPlace`
分辨兩者（「找附近有賣的店」變「導航過去」、「優點」變「在哪裡」）。

**這是刻意的權宜，不是沒想到。** 乾淨的做法是抽一個 `WheelEntry` 協定讓兩者各自實作，
但那要動 `WheelView` 與 `SpinRecord` 的編碼。等到店家模式需要顯示營業時間、評分
這些菜色沒有的東西時，再回來做那件事。

## 四、歷史紀錄的相容性

`SpinRecord` 是 SwiftData `@Model`。處理方式：

- `engineRawValue` 欄位**保留但不再寫入**。直接從 model 上拿掉會需要一次 migration，
  留著空字串則什麼都不用做。
- `prompt` 欄位沿用，改存條件摘要（「日式・清爽・宵夜」）而不是使用者打的那段話。
- **舊紀錄的候選清單會解不開。** 舊 JSON 沒有 `id` 與 `tags`，`JSONDecoder` 直接失敗，
  `items` 的 `try?` 會回空陣列。這是**刻意接受**的：為了幾筆舊紀錄寫一套相容解碼不值得。
  轉出的結果（`winnerName`）仍然看得到，只是「還原這組清單」對舊紀錄無效。

## 五、實跑驗收（iPhone 17 Pro 模擬器）

| 項目 | 結果 |
|---|---|
| `grep -r FoundationModels FoodRotate/` | 無結果，完全脫鉤 |
| 開 App | 轉盤已填滿，沒有載入畫面 |
| 篩「日式 + 無牛」 | 5 道日式全在盤上，第 6 格補外面的菜，無牛條件沒被違反 |
| 新增自訂料理選「義式」 | 出現在義式篩選結果，殺掉 App 重開仍在（設定頁顯示「目前可抽 51 道」） |
| 切「去哪吃」（定位設台北大安區） | 抓到鼎泰豐 258 公尺、天空興波、劉媽媽涼麵等實際店家 |
| 抽中店家 → 導航（Apple） | 正確導到座標，1.5 公里 |
| 抽中店家 → 導航（Google，模擬器未裝 App） | 自動退回網頁版並帶正確座標 |
| `foods.json` 資料檢查 | 50 筆、id 不重複、12 分類每類 ≥2 道、標籤全合法、優缺點數量與字數都在範圍 |

## 六、還沒做 / 已知限制

- **「去哪吃」沒有營業狀態。** MapKit 給不出來（見 3.8）。要真的有「營業中／已打烊」
  必須接 Google Places API，需要金鑰與計費。目前的替代是把電話擺在明顯的位置讓人自己問。
- **`foods.json` 沒有自動化的資料檢查。** 目前是靠一次性的 script 驗過，
  但那不在 build 流程裡。加一道菜打錯標籤名稱只會在執行期才發現（`FoodLibrary` 會 `fatalError`，
  至少不會靜默）。值得補一個 DEBUG 期的檢查。
- **「去哪吃」沒有時間上限。** 定位加地圖搜尋在訊號差的地方可以拖很久。
  已加的緩解是定位快取（120 秒內原地重搜不必重新定位）。
  曾一度加上 5 秒硬上限，後來依 kuoyo 指示拿掉。
- **店家模式的篩選只吃菜系那一列。** 「無牛」「便宜」「宵夜」沒辦法對店家生效，
  所以那幾列在這個模式下不顯示——列出來卻不起作用比不列出來更糟。
- **Android 移植還沒開始。** 這次只做到「不製造移植債」的程度。
